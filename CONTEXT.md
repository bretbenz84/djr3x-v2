# DJ-R3X v2 - Concise Project Context

This file is a compact handoff for future AI/development sessions. It explains what the project is, how to run it, the main architecture, and the current design assumptions. It intentionally omits long personality transcripts, old planning notes, and exhaustive behavior specs.

## Project Summary

DJ-R3X v2 is a local, voice-first droid brain inspired by Rex from Star Tours and Oga's Cantina. It runs on macOS, combines live audio, camera perception, face and voice identity, persistent memory, LLM conversation, TTS, games, music, and physical droid hardware control.

The core loop is:

1. Detect or receive a human utterance.
2. Resolve who likely spoke.
3. Interpret the intent.
4. Choose a local action, routed action, or LLM response.
5. Speak or log the response.
6. Save useful memory only when appropriate.
7. Emit telemetry showing what happened.

Primary workspace:

```bash
/Users/bbenziger/djr3x-v2
```

Use the project virtual environment:

```bash
source venv/bin/activate
venv/bin/python -m unittest discover -s tests
```

`pytest` is not assumed to be installed; use `unittest` unless the repo changes.

## Supported Runtime Modes

DJ-R3X is designed for macOS, preferably Apple Silicon.

| Mode | Command | Behavior |
| --- | --- | --- |
| Full voice mode | `python main.py` | Uses mic, wake word, transcription, speaker ID, TTS, camera, memory, and available hardware. |
| GUI mode | `python main.py --gui` | Adds the PySide6 dashboard while keeping normal audio behavior. |
| Text-only GUI mode | `python main.py --gui --noaudio` | Disables mic/audio/TTS calls. GUI text input is processed like speech and responses appear as text. |
| Jeopardy launch | `python main.py --jeopardy` | Starts directly in Jeopardy mode and skips normal startup introductions. |

Startup flags owned by `main.py`:

| Flag | Purpose |
| --- | --- |
| `-gui`, `--gui` | Open the optional dashboard. |
| `-jeopardy`, `--jeopardy` | Start directly in Jeopardy mode. |
| `-noaudio`, `--noaudio`, `--no-audio` | Disable microphone capture, wake word, audio output, and ElevenLabs calls. |

In no-audio mode, `main.py` sets runtime-only config values:

- `config.NO_AUDIO_MODE = True`
- `config.AUDIO_OUTPUT_SUPPRESSED = True`

It skips Whisper verification, audio stream startup, wake word listening, audio scene analysis, TTS prewarm, speaker-ID preload, startup/shutdown audio, listening chime, ElevenLabs fetches, and direct playback.

## Configuration And Secrets

Tracked configuration:

- `config.py`: tunable defaults, model names, thresholds, feature flags, servo defaults, latency settings.
- `.env.example`: host-specific config template.
- `apikeys.example.py`: API key template.

Untracked local/runtime files:

- `.env`: machine-specific camera, microphone, hardware ports, servo limit overrides.
- `apikeys.py`: OpenAI and ElevenLabs keys.
- `assets/memory/people.db`: local person database.
- `assets/audio/tts_cache/`: generated ElevenLabs cache.
- downloaded model assets.

Never commit real secrets, local databases, generated TTS cache, local music, or downloaded model files.

## Repository Map

Important entry points and modules:

```text
main.py                  Startup, CLI flags, service orchestration, shutdown.
state.py                 Runtime state machine.
world_state.py           Thread-safe shared perception/session state.
config.py                Tunable defaults and feature flags.

audio/
  stream.py              Mic stream and rolling buffer.
  vad.py                 Silero VAD.
  transcription.py       mlx-whisper plus OpenAI fallback.
  speaker_id.py          Resemblyzer voice embeddings and speaker matching.
  wake_word.py           OpenWakeWord loop.
  speech_queue.py        Prioritized response queue and playback/text completion.
  tts.py                 ElevenLabs TTS, cache, no-audio bypass.
  echo_cancel.py         Playback suppression/AEC state.
  scene.py               Background audio scene analysis.

intelligence/
  interaction.py         Main turn pipeline for speech and GUI text input.
  consciousness.py       Proactive loop, greetings, presence, empty-room behavior.
  dialogue_act.py        Cheap conversational-frame gate before executable actions.
  action_router.py       LLM action routing.
  command_parser.py      Fast/local command recognition.
  intent_classifier.py   Intent fallback and deterministic guards.
  llm.py                 Main LLM prompt assembly and response generation.
  local_llm.py           Ollama sidecar for low-latency local calls.
  empathy.py             Affect classification and emotional event handling.
  social_frame.py        Response shape/governance cleanup.

memory/
  database.py            SQLite connection, schema, migrations.
  people.py              People, face/voice biometrics, familiarity.
  facts.py               Person facts and observations.
  events.py              Upcoming/follow-up events.
  emotional_events.py    Sensitive/celebratory emotional memories.
  social.py              Inter-person relationship edges.

vision/
  camera.py              Camera capture.
  face.py                dlib face recognition.
  scene.py               Environment/scene analysis.
  pose.py                Body pose/gesture hooks when available.
  proxemics.py           Distance/space estimation.

gui/
  dashboard.py           PySide6 dashboard and text input surface.

features/
  dj.py                  Music playback.
  games.py               Game orchestration.
  jeopardy.py            Jeopardy mode.
  trivia.py              Trivia.

hardware/
  servos.py              Pololu Maestro and servo behaviors.
  leds_head.py           Head LED Arduino.
  leds_chest.py          Chest LED Arduino.
```

## Runtime Architecture

### Startup

`main.py` owns the process lifecycle:

1. Verify database schema and local configuration.
2. Initialize optional hardware; missing ports disable features gracefully.
3. Start audio, camera, awareness, consciousness, and interaction services.
4. Prewarm output and preload models when enabled.
5. Enter the main state loop until shutdown.

The local Ollama model `qwen2.5:1.5b` is preloaded for quick classifier/shaping tasks when configured.

### Speech And Text Turns

`intelligence/interaction.py` is the main turn pipeline.

Spoken turn:

1. VAD/wake or idle speech activation.
2. Transcribe audio.
3. Run speaker ID.
4. Fuse voice with visible/recent world state.
5. Run dialogue-act/contextual binding before executable routing.
6. Execute local handler or call LLM.
7. Queue speech/text output.
8. Extract memories after the turn unless suppressed.
9. Log `[character_loop]` telemetry.

GUI text turn:

1. `gui/dashboard.py` sends text to `interaction.submit_text(...)`.
2. The normal pipeline runs with transcription and speaker-ID bypassed.
3. Attribution prefers recent engagement, then a single unambiguous visible known person.
4. In `--noaudio`, responses are logged as text and no ElevenLabs call is made.

Use `interaction.submit_text(...)` for programmatic text injection. Do not invent a parallel text-only pipeline.

### Turn Routing Policy

The desired routing hierarchy is now:

1. Dialogue act: decide whether the utterance is answering Rex's last turn.
2. Contextual binding: bind answers to the correct person/frame, including multi-person cases.
3. Executable action gate: only run actions with strong, action-shaped evidence.
4. Conversation fallback: ambiguous turns stay conversational and can be handled by the main LLM.

`intelligence/dialogue_act.py` owns the cheap session-local frame check. Rex turns are recorded as `RexTurnFrame` objects with source, topic, target person, expected reply types, and blocked actions. This prevents normal replies such as "Yeah, that's not happening anymore" from being promoted into identity, memory, weather, game, or music commands.

`interaction.py` now has a central turn-policy gate for old fallback layers:

- Legacy `command_parser` claims go through `_legacy_command_execution_block_reason(...)`.
- Deterministic intent claims go through `_intent_execution_block_reason(...)`.
- Both gates consult the dialogue decision and `action_router.missing_required_evidence_reason(...)`.
- Legacy fuzzy command execution is disabled by default via `LEGACY_COMMAND_FUZZY_EXECUTE_ENABLED` being absent/false.
- The live interaction path uses `intent_classifier.classify_deterministic(...)`, so the intent-classifier LLM fallback is not called during normal turns.

When investigating false positives, search logs for:

- `[dialogue_act]`: whether a turn was bound to Rex's last frame.
- `[turn_policy] blocked ...`: an old command/intent claim was rejected before execution.
- `[action_router_audit]`: final routed action, legacy claim, allowlist/block reason, and executed path.

The failure mode to avoid: a normal contextual response gets a second chance in a later legacy layer and becomes a durable action. Do not add new command/intent bypasses after the dialogue gate unless they pass the same central evidence policy.

### Output

`audio/speech_queue.py` is the central response queue. It handles priority, coalescing, playback start callbacks, and completion.

`audio/tts.py` handles ElevenLabs cache lookup/fetch/playback. In no-audio mode, `speak()` and `ensure_cached()` return before network or playback work. By default `speak()` derives expressive ElevenLabs `voice_settings` from the line's emotion (`emotion_orchestrator.voice_settings_for_emotion`, backed by `config.TTS_VOICE_SETTINGS_*`); an explicit empathy/grief override passed by the caller takes precedence.

## Latency And Telemetry

The project now has explicit latency instrumentation.

Key logs:

- `[latency]`: stage timings inside a turn.
- `[ttfs]`: time to first response queued and first audio/text start.
- `[character_loop]`: full per-turn summary: speaker, interpretation, execution, output, memory suppression, timing.
- `[action_router_audit]`: final action routing result.

Recent latency architecture:

- `audio.speaker_id.preload()` runs at startup when `config.SPEAKER_ID_PRELOAD_ON_STARTUP` is true, removing first-turn Resemblyzer load cost.
- Slow-path acknowledgments (short "One sec." receipts for known-slow `general`/`memory`/`vision` paths) and the delayed latency filler (in-character "One sec, thinking." lines) are now **disabled by default** — `config.SLOW_PATH_ACK_ENABLED = False` and `config.LATENCY_FILLER_ENABLED = False`. They felt out of place, and the streaming answer path now gets Rex's real first sentence out fast, so the latency cover is unnecessary. The machinery and tunables (`SLOW_PATH_ACK_LINES`, `SLOW_PATH_ACK_EXPECTED_SECS`, `LATENCY_FILLER_LINES`, `SLOW_PATH_ACK_IN_TEXT_ONLY`) remain; flip either flag back to True to restore. The slow-path-ack tests enable the flag explicitly to keep covering the firing logic.
- End-of-speech wait `config.SILENCE_TIMEOUT_SECS = 0.6` (was 0.9): how long of sustained silence after the user stops before transcription begins. Lowered for responsiveness on every turn; raise toward 0.8 if slow / pausing speakers get cut off mid-sentence.

When assessing responsiveness, prefer TTFS/audio-start timings over total turn duration. Total duration includes how long Rex speaks.

## Identity And Multiple Speakers

Identity combines:

- Face recognition from `vision.face`.
- Voice embeddings from `audio.speaker_id`.
- Current visible people from `world_state`.
- Recent engaged speaker/session continuity.
- Conservative fallbacks when ambiguity is high.

Important behavior:

- A hard voice threshold prevents casual misidentification.
- A softer session-sticky threshold can keep continuity during an active exchange.
- If only one known person is visibly engaged, world-state continuity may override weak voice scores.
- If multiple known faces are visible and voice confidence is low, the system can create an anonymous voice label like `unknown_voice_1` instead of forcing a person match.
- Unknown voices can be tracked within the session even before a name is known.
- Directional audio intelligence is a future design target, not currently implemented.

Recent introduction repair:

- Relationship-only intros such as "I'd like you to meet my sister" open a pending introduction slot even if no unknown face is visible at that exact instant.
- If the follow-up name matches a known visible/recent person, Rex should welcome/link that existing person rather than treating it as a rename of the current speaker.
- This prevents cases like known face Jennifer being interpreted as "rename Bret to Jennifer."

## Memory Model

Memory is stored in SQLite under `assets/memory/people.db`.

This local DB is **disposable development/test data** — it is untracked and may be
freely reset, cleaned, or fully erased during development (e.g. to clear stale rows
like duplicate test events). It is not precious user data and does **not** need to
be backed up before changes; wipe or edit it directly when iterating.

Main concepts:

- People records with names, biometrics, familiarity, and relationship metadata.
- People records include `lifetime_greeting_count` and `last_greeted_at` for grounded answers to greeting-count questions going forward.
- Face and voice biometric rows for identity matching.
- Person facts, preferences, interests, and events.
- Emotional events for celebrations, grief, wins, worries, and follow-ups.
- Social relationship edges between people.

Memory extraction runs after turns and may call OpenAI. It should be suppressed for commands or corrections where learning would be wrong. Forget/discard commands exist and should be respected.

Do not treat every utterance as a permanent fact. Prefer explicit user statements, repeated stable preferences, and meaningful life events.

## Proactive Behavior

`intelligence/consciousness.py` runs background awareness and proactive behavior.

Important proactive cases:

- Startup greeting: if a known person is in front of the camera, Rex should greet them by name.
- Empty-room startup: if nobody is visible, Rex can make a short snarky empty-room remark.
- First-sight celebration/event check-ins can happen when a remembered relevant event exists.
- Holiday-plan proactivity is major-holiday-only by default; minor public holidays require `HOLIDAY_PLANS_INCLUDE_MINOR = True`.
- The action governor arbitrates proactive candidates so Rex does not stack too many remarks.

If startup greetings feel wrong, inspect face detection timing, world-state updates, and action-governor candidate selection.

## Social Conversation Layers

The project still has several layers that shape final speech, but executable turn ownership should remain centralized:

- Dialogue-act frame gate for contextual replies.
- Action router for executable intents and evidence policy.
- Command parser for legacy exact/pattern commands after turn-policy gating.
- Deterministic intent classifier for local data/tool answers after turn-policy gating.
- Conversation agenda/social frame to keep responses short, relevant, and socially targeted.
- Empathy classifier for emotional mode and event capture.
- Memory injection for known people.

For group settings, the desired future direction is not "ignore all crosstalk." It is social turn triage: decide whether a line is directed at Rex, overheard but relevant, background crosstalk, or a group-addressed turn.

## Hardware

The robot can run without hardware attached.

Optional hardware:

- Pololu Maestro servo controller.
- Head LED Arduino.
- Chest LED Arduino.
- Camera.
- Microphone or ReSpeaker Lite.
- Speakers/audio output.

Missing serial ports are warnings, not fatal errors, unless a feature explicitly requires hardware safety validation. Servo min/max overrides belong in `.env`, using microsecond values from the Maestro Control Center. Do not connect live servos until safe travel limits are configured.

## GUI

The PySide6 dashboard is optional and launched with `--gui`.

Important GUI behavior:

- It mirrors runtime state and conversation logs.
- Its text input can submit turns through `interaction.submit_text(...)`.
- With `--gui --noaudio`, the app becomes a text-only test interface for the full conversation/router/memory pipeline.

## External Services

OpenAI is used for main chat, vision/scene analysis, extraction, and classifiers depending on path.

ElevenLabs is used for TTS in audio mode only. No-audio mode must not call ElevenLabs.

Ollama/local LLM is used as a low-latency sidecar for quick local tasks when configured.

Network calls may dominate response latency. Prefer local fast paths for clear commands, short acks for slow paths, and telemetry-driven optimization.

## Development Rules

- Use existing project patterns before adding new abstractions.
- Keep changes scoped; this repo has many interacting behavior layers.
- Do not revert unrelated dirty worktree changes.
- Preserve privacy and safety gates around memory, vision snapshotting, and identity changes.
- Avoid adding new permanent memory writes unless the user clearly intends Rex to remember something.
- In no-audio mode, avoid any mic, wake word, TTS, or ElevenLabs work.
- For frontend/GUI changes, keep text input routed through the same interaction pipeline as speech.

Useful commands:

```bash
venv/bin/python -m unittest discover -s tests
venv/bin/python main.py --gui --noaudio
venv/bin/python main.py --gui
venv/bin/python main.py
```

## Recent Architecture Changes To Preserve

- Command-line no-audio mode with `--noaudio`, `--no-audio`, and `-noaudio`.
- GUI text input routed through `interaction.submit_text(...)`.
- TTS and speech queue bypass in no-audio mode.
- Speaker-ID encoder preload at startup.
- Exact TTFS logging.
- `[character_loop]` per-turn telemetry.
- Slow-path acknowledgments (general/memory/vision) and the latency filler exist but are now **disabled by default** (`SLOW_PATH_ACK_ENABLED` / `LATENCY_FILLER_ENABLED` = False) — see the Latency And Telemetry section.
- Action-router guardrails downgrade common false positives: ongoing status updates are not event cancellations, pronoun-only fragments are not introductions, named holiday explanations are not date queries, and relationship-score questions outside games route to memory.
- Dialogue-act frame gate (`intelligence/dialogue_act.py`) protects normal replies to Rex's last turn before routers can claim them.
- Central turn-policy gates in `interaction.py` now require legacy command-parser and deterministic intent claims to pass dialogue context plus action-shaped evidence.
- Intent classifier live path is deterministic only; no extra LLM classifier call is added on normal turns.
- Stale event-cancellation acknowledgments are deterministic to avoid weird generated lines and extra LLM latency.
- Regression corpus for misroutes lives under `tests/fixtures/misroute_replays.json`; add false-positive examples there when they appear in live logs.
- Memory-query grounding for self relationship metrics and greeting counts.
- Minor public holiday proactive questions are gated behind `HOLIDAY_PLANS_INCLUDE_MINOR`.
- Introduction handling that links known visible/recent people instead of renaming the current speaker.
- README startup flag documentation.
- Expressive TTS voice: `tts.speak()` derives ElevenLabs `voice_settings` from the turn's emotion frame (`emotion_orchestrator.voice_settings_for_emotion`) whenever the caller passes no explicit override, so normal lines carry an expressive baseline instead of the voice clone's flat (style≈0) defaults. Tunables: `config.TTS_VOICE_SETTINGS_BASELINE`, `TTS_VOICE_SETTINGS_BY_STYLE` (keyed by emotion `voice_style`), and the `TTS_EXPRESSIVE_VOICE_ENABLED` kill switch. `TTS_MODEL_ID` is `eleven_multilingual_v2` because it honors `style` strongly (turbo_v2 applies it only weakly). The resolved voice settings and `model_id` are part of the TTS cache key, and `is_cached()`/`ensure_cached()` take an `emotion` arg so ack/wake prefill keys match live playback. Do not regress `speak()` back to sending `voice_settings=None` on normal turns, and keep empathy/grief overrides taking precedence.
- Streaming answer → TTS: on audio turns the conversational answer streams sentence-by-sentence (`interaction._stream_and_speak_sentences`) — the first sentence is spoken as soon as the LLM produces it; the rest queue behind it through the single one-at-a-time speech queue, so Rex never overlaps himself. Per-sentence safety governance is preserved (`social_frame.govern_stream_sentence`, `comedy_modes.polish_stream_sentence`), the one-question cap holds across the stream, and a fallback speaks a whole-reply govern result if every sentence is filtered out. Flagged by `config.LLM_STREAMING_TTS_ENABLED` (default on); bypassed in no-audio mode. `speech_queue.enqueue`/`tts.speak` take `log_text=False` so a streamed turn is logged once, not per sentence.
- WorldState lost-update race fix: `world_state.mutate(field, fn)` runs the whole read-modify-write under the lock. Every `people` writer (face recognition, pose, expression loops, the identity binders, face-mood) goes through it instead of `get()`+`update()`, so concurrent threads can't silently revert each other (the cause of `person_db_id` flicker / misattribution). Slow work (face-DB lookups) stays outside the lock. Use `mutate()` for any new people / self_state writers.
- OpenAI connection warmup at startup: `llm.warmup()` + `action_router.warmup()` (separate clients) run in a background thread, gated by `config.OPENAI_WARMUP_ON_STARTUP`, so the first turn doesn't pay cold TLS/HTTP setup.
- Stale-event-cancel guard: `memory.events.looks_like_cancellation` now requires a cancellation phrase AND the absence of a false-positive idiom ("not going to lie", "not doing too bad", "on my way"), so a conversational outcome reply can't durably mark a remembered event canceled. This gate protects both the follow-up handler and `_cancel_stale_event_memory`.
- Identity sub-0.75 floors require the top voice candidate to BE the attributed person: the single-visible-continuity and multi-visible-recent floors now check `raw_best_id == person` (the engaged-visible and grief floors already did), so a second speaker in a one-on-one frame is treated as off-camera-unknown instead of pinned on the engaged person.
- Bug fixes to preserve: `SCENE_MUSIC_BAND_ENERGY_MIN = 2e-6` (was `2-6`, i.e. −4, which made every band count as active → music always "detected"); dead `GUI_SHOW_FPS` removed; `social_frame` optional-lookup `except` handlers now log at debug instead of swallowing silently.
- Event follow-up resolution (fixes the "Rex obsessively re-asks how the concert went" loop): a reply that an event never happened ("I never went / didn't go" — `interaction._followup_event_did_not_happen`) resolves a pending follow-up instead of being held open as a repair, and an unanswered follow-up is dropped after `config.FOLLOWUP_MAX_HELD_OPEN_TURNS` (default 1) so it stops being re-injected into the agenda as Rex's "unresolved question". The follow-up handler lives in `interaction.py` (search `_awaiting_followup_event`); the proactive ask comes from `consciousness` Priority 2.5 (`get_pending_followups`).
- The "one sec" fillers (slow-path ack + latency filler) are disabled by default and `SILENCE_TIMEOUT_SECS` is 0.6 — see the Latency And Telemetry section. Do not re-enable the fillers without a reason.
- The local `assets/memory/people.db` is disposable dev/test data — reset, clean, or wipe it freely (no backups needed) when iterating; see the Memory Model section.
- Upstream work merged onto `main` alongside this voice/latency/identity effort (authored separately, around commit `ffa068e`): special per-person greetings / identity intros in `intelligence/person_specials.py`, delayed last-name prompts, sleep wake-word fallback, turn-completion for embedded answer clauses, and head-LED speech-stop stabilization.
- Comedy-forward rebalance (LATEST intent — supersedes the friendly-interview tuning below): live runs read as a polite, curious interviewer, not the roast-first droid the core prompt describes — every line opened with "Ah,", validated instead of needled, and ended with a profile question. The fix is layered prompt engineering, NOT a personality-param change (`roast_intensity` was already 90):
  - `social_frame.build_directive` was the dominant softener (it overrides personality, "obey this social frame first"). Its "normal" roast rule said "keep it socially on-target" / default "mild"; now it's **ROAST-FIRST: open with a sharp, SPECIFIC, earned jab, commit to the bit, punch up**. Tender levels ("none"/"light") and the affect/sensitivity/boundary/kids gates in `_roast_level` are untouched — sharpening only applies to happy consenting-adult turns.
  - Appearance/visual roasting is now allowed material: `_visual_allowed` returns True on normal upbeat turns (gated by `config.VISUAL_ROAST_ON_NORMAL_TURNS=True`), not only when the human says "look at this", and the visual directive invites roasting the outfit/drink/clutter/dog "when it fits." Still suppressed for sad/sensitive/support/micro/kids.
  - `REX_CORE_PROMPT` gained an anti-formula paragraph: comedy first, lead with the bit, **ban "Ah,"/"Oh,"/"Well, well, well" openers, never start two replies the same way, never narrate the wit** ("witty repartee"), don't end every turn with a question.
  - `llm.assemble_system_prompt`: the per-person rule is now "roast comedian, not friendly interviewer" (curiosity is seasoning, not the meal); the personality-parameter block is rendered as actionable dials ("high roast/sarcasm = be sharp; low agreeability = push back") so the 90 actually drives behavior. `agreeability` default 70 → **35**.
  - Question cadence pulled back so it stops feeling like an interview: `QUESTION_BUDGET_MAX_QUESTIONS` 6 → 3, `QUESTION_BUDGET_ENGAGED_EXTRA` 3 → 1, and every `conversation_agenda` purpose line now leads with a reaction/roast with the question demoted to an optional "fold in." Profile-building still happens, just behind the comedy.
  - NOTE: personality params live in the (disposable) `people.db` `personality_settings` table and override config defaults once written — if the robot's DB has low roast/high agreeability, reset those rows or set via voice to feel the change. Tests: `tests/test_comedy_modes.py::RoastForwardDirectiveTests`.
- Friendlier, profile-building conversation (earlier intent, now rebalanced behind the comedy-forward note above — keep the machinery, not the every-turn-interview balance): R3X asks about hobbies / interests / music / preferences with pointed follow-ups (not just plans) and adapts to each person's worldview, while keeping his roast-first voice. Most machinery already existed but was gated off; the key levers:
  - `config.REACTIVE_FRIENDSHIP_QUESTIONS_ENABLED = True` (was absent → False) makes `conversation_agenda` weave a `QUESTION_POOL` question into normal reactive turns instead of falling through to "just respond, don't pivot". Plus `TIER_MAX_DEPTH` acquaintance → 2 (unlocks hobbies/obsession) and `LOW_MEMORY_PROFILE_MAX_FACTS` → 12 (keeps asking); the idle question prefix is warmed.
  - A per-person rule in `llm.assemble_system_prompt` (the `if person_id is not None:` block) instructs Rex to be warm/curious, ask pointed follow-ups about people's interests, and **adapt to their job/worldview/interests** (engage a scientist vs a person of faith differently) — explicitly WITHOUT softening his roast-first voice. Per-person roast control (`social_frame._roast_level`: tier + boundaries + preference facts) is untouched.
  - `llm.extract_facts` now captures religious/scientific/values cues as a `worldview` fact; `memory/facts.py` makes `belief`/`worldview` high-importance and `worldview` permanent so it ranks into the prompt and actually drives the adaptation. (Hobbies/music/interests were already extracted to `person_interests`/`person_preferences`.)
  - Proactive small-talk (`consciousness._do_small_talk_question`) now leads with curiosity about the person (hobbies, music, what they're into) instead of defaulting to their schedule.
  - Cold opens stay casual (do not regress): the first-sight/startup greeting must NOT lead with a profile question ("What kind of music are you into?") — that's an awkward intake-form opener. `config.STARTUP_PROFILE_QUESTION_ENABLED = False` gates `consciousness._pick_startup_profile_question` so the opener falls through to the mood check-in or the `FIRST_GREETING_STEERING_PHRASES` greeting ("What are you up to today?" / "how are you?"). Profile-building still happens once the conversation is rolling (reactive friendship questions) and during lulls (`LOW_MEMORY_IDLE_QUESTION_ENABLED`). The downstream profile-question machinery is untouched — flip the flag True to restore the old behavior.
- "Muzzle" means stop/decline the music (do not regress): Rex's music-offer line is "...play some X, or keep the jukebox muzzled?", so a reply like "let's keep it muzzled for now" must read as a decline. `interaction._DECLINE_PAT` now matches `muzzle(d)`, `keep it off/down/quiet/muzzled`, `no music`, `hold off`, `let's not`, etc., and `_classify_consent` checks decline before affirm — otherwise the leading "let's" was matching `_AFFIRM_PAT` and Rex played music ("Yes detected"). `command_parser` also maps "muzzle"/"muzzle the music/jukebox/song" → `dj_stop` for direct commands.
- Wake-word barge-in during DJ playback (do not regress): a wake word while music plays now STOPS the track and listens, instead of being swallowed. Previously `_dj_suppresses_conversation()` made the IDLE/ACTIVE wake handlers log "wake ack suppressed during DJ playback" and `continue`, so the user had no voice way to stop the music (only killing the process). `interaction._stop_dj_for_wake()` stops the DJ and clears the post-playback suppression tail; both wake handlers call it before going ACTIVE / acking. Note: VAD can't hear over playback because `_chunk_for_vad` attenuates the mic by `AEC_SUPPRESSION_FACTOR` (0.05) whenever DJ `_playing` — so the wake word, not VAD, is the intended interrupt during music.
- Wake word over music: loud playback masks the phrase, so `audio.wake_word._threshold(model, dj_playing=True)` drops the bar by `config.WAKE_WORD_DJ_PLAYBACK_THRESHOLD_DELTA` (0.15 → 0.35 effective) floored at `WAKE_WORD_MIN_THRESHOLD` (0.30). Tune the delta down toward 0.0 if music ever false-triggers a wake. The detection loop checks `dj.is_playing()` once per chunk (lazy import, no cycle).
- Post-question handoff stickiness (fixes "I answer right after Rex's question but he doesn't hear me"): a streamed reply fires `_apply_post_tts_handoff` per sentence AND once for the whole reply. If Rex asked a question but his trailing sentence is a statement, that last handoff used to flip back to the long flush window (`POST_SPEECH_LISTEN_DELAY_SECS` 0.35 + buffer flush), deleting the human's immediate answer. Now any question handoff sets `interaction._last_fast_handoff_at`, and a non-question handoff within `config.POST_QUESTION_HANDOFF_STICKY_SECS` (1.5s) is kept responsive (short delay, no flush). NOTE the deeper half-duplex limit remains: `VAD_BARGE_IN_ENABLED=False` + AEC mic suppression mean answers spoken *while Rex is still talking* are still lost — the wake word is the only mid-speech interrupt. Tests reset the module global in `setUp` (it's wall-clock-keyed; a mocked `time.monotonic` otherwise leaks stickiness across tests).
- Crosstalk suppression (best-effort, do not over-tighten): with an always-on mic Rex treats the user talking to a partner / another room as a turn aimed at him. `interaction._looks_like_third_party_crosstalk` is a HIGH-PRECISION/low-recall gate — it suppresses only the clearest third-party lines (partner endearments `babe`/comma-set-off `honey`/`sweetheart`/…, and "love you (too)" at a clause edge) and NEVER when a Rex token (`rex`/`r3x`/`dj`/`droid`/`robot`/`jukebox`) is present, so it can't make Rex ignore real input. Gated by `config.CROSSTALK_SUPPRESSION_ENABLED`; on a false IDLE activation Rex drops straight back to IDLE. It deliberately won't catch ambiguous overheard lines — fuller addressee/social-turn triage (gaze, voice-vs-visible, an addressee classifier) is still future work, and body-pose "facing" cues are unavailable on the current mediapipe build.
- No-response quip rhetorical guard: `_question_sentence_expects_response` now returns False for rhetorical "who doesn't / wouldn't / hasn't … ?" forms, so Rex's own flourishes ("who doesn't appreciate a droid that can sing?") no longer arm the "Guess that landed in the cargo bay" no-response quip (it misfired twice in live logs).
- Silence drives conversation, doesn't end it (do not regress): when the user just goes quiet (no goodbye), Rex now re-engages instead of signing off. Two root causes were fixed: (1) `end_thread._CLOSURE_PAT` matched bare politeness like "thank you" anywhere, so a compliment reply ("Well thank you") falsely armed the 35s end-of-thread grace that muzzled all proactive purposes — soft acks ("thanks"/"got it"/"cool"/"sounds good") were moved OUT of `_CLOSURE_PAT` and now only close via `_SHORT_ACK_PAT` (whole-utterance ack AND Rex just asked a question); real goodbyes ("bye", "gotta go", "that's all") still close. (2) The only thing firing on pure silence was the idle-timeout outro. Added `interaction._maybe_idle_banter` (config `IDLE_BANTER_*`): after `IDLE_BANTER_SECS` of silence it proactively re-engages up to `IDLE_BANTER_MAX_PER_STRETCH` (2) times — alternating between asking about the user and Rex volunteering his own opinion/preference — then lets the outro close if still silent. `_idle_banter_count` resets in `_begin_user_turn`. `CONVERSATION_IDLE_TIMEOUT_SECS` 30 → 45 so banter has room before timeout. Works for well-known people (unlike the interest/low-memory idle paths). Tests: `EndThreadClosureNarrowingTest`, `IdleBanterTest`.
- Face-tracking responsiveness (fixes "Rex takes many seconds to turn and face me"): the head-pose loop (`consciousness._step_face_tracking`, ~12.5 Hz) is a closed loop — camera on the head. The centering **gain** (`FACE_TRACKING_CENTERING_GAIN=0.65`) is roughly matched to the camera FOV, so the per-tick *target* was about right; the lag came from **slew-rate throttling**. Logs showed +54 qus/tick neck moves = `FACE_TRACKING_NECK_MAX_STEP_QUS (120) × FACE_TRACKING_LIVE_BOX_DAMPING (0.45)` — and that damping hits ~11 of every 12 ticks (dlib detection is ~1 Hz; optical-flow boxes carry the rest), so the neck topped out near 675 qus/s and a big move took 2-4 s. Fix is pure slew-rate tuning (all `.env`-overridable): neck/lift/tilt max-step 120/80/40 → **280/190/95**, live-box damping 0.45 → **0.8**, servo speed/accel 70/10 → **95/32**, `TRACKING_SMOOTHING_FACTOR` 0.55 → **0.45**. Deliberately UNCHANGED: the geometric `FACE_TRACKING_CENTERING_GAIN` and `FACE_TRACKING_REVERSAL_DAMPING` (0.35, the anti-oscillation safety net) — raising the gain would overshoot/hunt. Max-step stays under the servo's per-tick travel at the new speed so the loop (which reads back *commanded*, not proprioceptive, position via `_current_servo_position`) doesn't outrun the real head. If the head hunts/overshoots on hardware, dial `FACE_TRACKING_LIVE_BOX_DAMPING` and the max-steps back down in `.env`. Guarded by `tests/test_face_tracking.py::test_live_tracked_edge_face_slews_neck_responsively`.
- Supervisor wake detection — ONNX-only, and the real root cause (do not regress to VAD/Whisper/RMS): `rex_supervisor.py` just runs the `wakeuprex.onnx` openWakeWord model on 80 ms mono frames and fires when the score crosses `REX_SUPERVISOR_WAKE_THRESHOLD` (0.5). No VAD, no Whisper, no transcription, no RMS gate — the robot is OFF while the supervisor listens, so the only job is to spot one wake word and launch `main.py`. The long-running "I said wake up Rex and nothing happened" / "the ONNX model is unreliable" symptom was NOT a weak model — it was an **input-scale bug**: openWakeWord's melspectrogram is trained on int16 PCM (±32767), but sounddevice returns float32 in [-1, 1]. Feeding the raw float makes the model see near-silence and every score pins at ~0.001. `_to_oww_input(mono)` rescales each frame to int16 before `predict()`; a clean "wake up rex" then scores ~0.99 (measured 0.997 live on the ReSpeaker Lite). Earlier VAD+Whisper and RMS-gate "fixes" were elaborate workarounds for this one-liner and have been removed. NOTE: the main app's `audio/wake_word.py` had the SAME input-scale bug — it fed float32 [-1,1] straight to `predict()`, so its ONNX wake words never fired (scores ~0.001 vs the 0.5 thresholds; likely why the SLEEP-wake VAD+Whisper fallback was added). **FIXED**: `audio/wake_word._to_oww_input(mono)` now rescales each frame to int16 (`*32767`, clipped) exactly like the supervisor, and `_detection_loop` feeds that to `predict()`. The `WAKE_WORD_THRESHOLDS` (all 0.5, floor 0.30 during DJ) were already written for the int16 regime, so no threshold change was needed. Unlike the supervisor, the main loop still fires on a SINGLE frame ≥ threshold (no consecutive-frame guard) — now that scores are real, watch for false triggers on loud speech/TV; if they appear, port the supervisor's `REX_SUPERVISOR_WAKE_CONSECUTIVE`-style guard and/or nudge `WAKE_WORD_THRESHOLD` toward 0.6. The `_threshold()` getattr fallbacks for `WAKE_WORD_DJ_PLAYBACK_THRESHOLD_DELTA`/`WAKE_WORD_MIN_THRESHOLD` were also corrected from `0.0` to the real defaults (0.15/0.30) so the DJ-ducking floor survives a missing config key. WAKE-DURING-SPEECH IS INTENTIONAL (do not "fix" by suppressing it): the wake word is the mid-speech interrupt — you bark a wake word to cut Rex off when he's long-winded — and it deliberately reads the shared rolling audio buffer (one mic consumer, no second input stream). An earlier change that gated detection off during Rex's own TTS (`_own_speech_suppresses_wake`) was WRONG and has been reverted; it killed the interrupt feature. THE REAL CRASH FIX (`audio/sd_guard.py`): once the wake word fired during playback, the barge-in does a cross-thread `sd.stop()` (speech_queue preempt + `interaction._speak_blocking`) and immediately replays the "what?" ack — and re-initializing PortAudio's single global CoreAudio stream mid-teardown hard-crashed the process (`Trace/BPT trap: 5`, observed on a second Mac). `sd_guard.install()` (called once at the top of `main._run_controller_startup`, before any playback) wraps `sd.play`/`sd.stop` with a shared re-entrant lock so they never overlap, and holds a settle (`config.AUDIO_PLAYBACK_STOP_SETTLE_SECS`, default 0.05s) after a stop so the device releases before a replay's `play()` runs. `sd.wait()` is deliberately NOT wrapped — it must stay interruptible by a cross-thread stop (that's how barge-in works). This makes every `sd.play`/`sd.stop` site crash-safe without a 10-site sweep. **SETTLE-SCOPE FIX (do not regress — caused startup TTS pauses/glitching/echoes):** `sounddevice.play()` calls `stop()` INTERNALLY before starting every clip (`_CallbackContext.start_stream`), so the naive guard applied the 50 ms settle to EVERY play (and every streamed sentence) → a gap-before-every-clip that glitched playback. Fixed with a per-thread `_local.in_play` flag set around `_orig_play`: `_guarded_stop` skips the settle when called from within a play on the same thread (the internal stop), and only settles for an explicit barge-in stop (different thread / not in_play). Also the AEC reference-capture in `_guarded_play` is now gated on `AEC_SOFTWARE_ENABLED` so it adds zero overhead (and no `import aec`) in the normal off path. Verified: normal play ≈ 0 ms added, explicit barge-in stop still ≈ settle. NOTE: native/timing-dependent crash — validate on the Mac that reproduced it; raise the settle toward 0.1 if it still trips. Self-triggering on Rex's OWN voice (the wake path isn't echo-cancelled) may still cause the occasional false barge-in; if it's annoying, add a consecutive-frame guard (keeps real barge-in, rejects 1-2-frame self-echo spikes) rather than disabling wake-during-speech. Tests: `test_sd_guard_serializes_and_settles_stop`. MASKING / mid-speech interrupt (the hard half-duplex limit): in live runs the wake word fired NOTHING for ~20s while Rex streamed a reply, then scored 0.99 the instant he stopped — i.e. Rex's own playback through the speakers acoustically MASKS the user's "hey rex" in the (raw, un-attenuated) wake buffer, so it can't cross the bar until he's quiet. Same reason a fast follow-up's leading words are dropped from transcription ("tell me about star trek…" → "star trek…"): the VAD path IS AEC-attenuated and the post-TTS handoff flush eats the overlap. Mitigation: `_threshold(..., tts_playing=...)` now drops the bar during Rex's own TTS by `config.WAKE_WORD_TTS_PLAYBACK_THRESHOLD_DELTA` (default 0.15 → 0.35, floored at `WAKE_WORD_MIN_THRESHOLD` 0.30), mirroring the DJ delta — `tts_playing = (not dj_playing) and echo_cancel.is_suppressed()`. A `[wake_diag] masked wake near-miss …` log (rate-limited, score ≥ 0.2) surfaces the loudest below-threshold score during playback so the masking is measurable: peaks near the bar → raise the delta; peaks near zero → the voice is fully masked and only true AEC (the unused `echo_cancel.add_reference` hook), mic placement, or lower playback volume will allow a mid-speech interrupt. The threshold drop can't go below the 0.30 floor, so it only helps when the masked score reaches it. Tests: `test_wake_threshold_drops_during_rex_tts_so_user_can_interrupt`. `--meter` now shows the live ONNX score so you can confirm detection. `[diag]` every 5s shows peak ONNX score + mic RMS. False-trigger hardening (do not regress — the supervisor was firing on background TV): a real "wake up rex" pegs the model near 1.0 for ~10 consecutive frames while TV/ambient tops out ~0.12 and phonetic near-misses are 1-2 frame spikes, so (1) the threshold default is **0.7** not 0.5 (`REX_SUPERVISOR_WAKE_THRESHOLD`), and (2) firing requires a sustained run of frames over the bar, default **3** (`REX_SUPERVISOR_WAKE_CONSECUTIVE`), not a single frame. Measured live: legit fires score 0.90-0.999; the one false fire on TV scored 0.608 (now blocked by both guards). Knobs: `REX_SUPERVISOR_WAKE_THRESHOLD`, `REX_SUPERVISOR_WAKE_CONSECUTIVE`. Tests: `SupervisorInputScalingTest` (int16 scaling + no VAD/Whisper/RMS machinery), `test_threshold_env_override`, `test_consecutive_frame_default_and_override`.
- Always-on wake-word supervisor + single-instance lock (see `docs/supervisor.md`): a tiny LaunchAgent process `rex_supervisor.py` runs the whole login session and listens for "wake up Rex" (see the wake-detection note above); on detection it launches `venv/bin/python main.py`. It is self-contained — NO project `config`/`apikeys` import (so it starts without keys), reads the mic from `.env` directly, and reuses the bundled openWakeWord feature models in `assets/models/wake_word/_openwakeword_resources/`. Coordination that prevents a double-launch: `main.py` holds an `flock` single-instance lock (`utils/single_instance.py`) for its ENTIRE lifetime — **including while asleep** — and the supervisor stays dormant (releases the mic, just polls) whenever `single_instance.is_held_by_other()` is true. So an asleep controller (which only wakes via its OWN internal `wakeuprex` detector, `wake_word.py` SLEEP state) is never duplicated; the supervisor only listens when no controller exists. The `flock` auto-frees on crash, so the supervisor self-recovers. Lock path is `DJR3X_LOCK_PATH` else `<tmpdir>/djr3x-main.lock`; `DJR3X_SKIP_SINGLE_INSTANCE=1` bypasses for dev runs. "Shut down"/"shut down Rex"/"power down" is now SEPARATE from "go to sleep": `command_parser.is_standalone_shutdown_command` + the `shutdown` key now routes to a real `system.shutdown` action (was collapsed into `system.sleep`) that sets `State.SHUTDOWN` (clean exit → supervisor resumes). Both sleep and shutdown require a short standalone phrase, so "shut down the music"/narration won't power off. Install: `scripts/install_supervisor.sh`. Tests: `test_single_instance.py`, `test_rex_supervisor.py`, `ShutdownVsSleepSplitTests`. macOS only (uses `fcntl.flock`); the lock module would need a Windows shim if ever ported.

- Same-day repeat-visit banter ("oh, it's you again"): when the same person summons Rex more than once in one local day, the startup greeting opens with a short roast about the repeat visit, then drops into normal conversation — instead of a clean greeting as if he hasn't seen them. `memory.people.record_greeting` now maintains a per-local-day tally (`greetings_today` / `greetings_today_date` columns on `people`, additive migration + base schema in `setup_assets.py`); `greetings_today_count(person_id)` returns prior greetings TODAY (stale days read 0). In `consciousness` the first-sight greeting chain has a new **Priority 3.5** tier (`_build_same_day_return_prompt`, gated by `config.PRESENCE_SAME_DAY_RETURN_ENABLED=True`): if `greetings_today_count >= 1` it fires the "you again" roast and escalates with the count ("3rd time today"). It sits below the memory/care tiers (emotional check-in, birthday, celebration, follow-up, anticipation — "care before the bit") but above the generic recent-return/mood/plain greeting. The greeting label contains "startup"+"return" so it counts as a greeting and the tally escalates across the day. Tests: `test_same_day_return.py`.
- Always-on wake-word supervisor + single-instance lock (see `docs/supervisor.md`): a tiny LaunchAgent process `rex_supervisor.py` runs the whole login session and listens for "wake up Rex" (see the wake-detection note above); on detection it launches `venv/bin/python main.py`. It is self-contained — NO project `config`/`apikeys` import (so it starts without keys), reads the mic from `.env` directly, and reuses the bundled openWakeWord feature models in `assets/models/wake_word/_openwakeword_resources/`. Coordination that prevents a double-launch: `main.py` holds an `flock` single-instance lock (`utils/single_instance.py`) for its ENTIRE lifetime — **including while asleep** — and the supervisor stays dormant (releases the mic, just polls) whenever `single_instance.is_held_by_other()` is true. So an asleep controller (which only wakes via its OWN internal `wakeuprex` detector, `wake_word.py` SLEEP state) is never duplicated; the supervisor only listens when no controller exists. The `flock` auto-frees on crash, so the supervisor self-recovers. Lock path is `DJR3X_LOCK_PATH` else `<tmpdir>/djr3x-main.lock`; `DJR3X_SKIP_SINGLE_INSTANCE=1` bypasses for dev runs. "Shut down"/"shut down Rex"/"power down" is now SEPARATE from "go to sleep": `command_parser.is_standalone_shutdown_command` + the `shutdown` key now routes to a real `system.shutdown` action (was collapsed into `system.sleep`) that sets `State.SHUTDOWN` (clean exit → supervisor resumes). Both sleep and shutdown require a short standalone phrase, so "shut down the music"/narration won't power off. Install: `scripts/install_supervisor.sh`. Tests: `test_single_instance.py`, `test_rex_supervisor.py`, `ShutdownVsSleepSplitTests`. macOS only (uses `fcntl.flock`); the lock module would need a Windows shim if ever ported.

- Listening motion (so Rex isn't frozen while he thinks): the moment VAD hears the user (`interaction._begin_user_turn`), `servos.start_listening_motion()` runs gentle "I'm listening / processing you" body language for the whole transcription→LLM→TTS wait, then `_end_user_turn` (or `begin_speech_motion`'s hand-off) stops it. Deliberately subtler/slower than speech motion: small downward "mhm" head nods orbiting the current gaze, a slow visor flutter, and occasional small elbow/hand/hero-arm shifts (`servos._listening_targets` per beat; a background `_listening_loop` thread). To avoid fighting the head, **breathing_thread and `consciousness._step_face_tracking` both yield while `servos.listening_motion_active()`**, and the instant Rex speaks `begin_speech_motion` calls `stop_listening_motion`. Safety net: the loop auto-stops after `SERVO_LISTENING_MAX_SECS` (20s) so a missed stop can never strand face tracking. Gated by `config.SERVO_LISTENING_MOTION_ENABLED`; all amplitudes/cadence are `SERVO_LISTENING_*` tunables. NOTE for tests on a hardware-attached box (SERVOS_ENABLED=True): anything calling `_begin_user_turn` spawns the real thread, so stub `servos.start_listening_motion`; and first-sight greeting priority-chain tests must also mock `consciousness._same_day_return_count` (Priority 3.5 reads the live people DB). Tests: `test_listening_motion.py`.

- Eye blink fix (Rex stopped blinking after his first spoken line): the head Arduino (`arduino/head_nano`) blinks the eyes autonomously, but only while `eyesActive==true`, which `SPEAK_STOP` and `OFF` set FALSE and only `ACTIVE`/`EYE:` set true again. The live speech path (`audio/tts.py` `_play`, and `main.py`'s direct-clip player) ends every utterance with `leds_head.speak_stop()` and — unlike the chest, which gets `leds_chest.active()` — never re-armed the eyes, so after the first response blinking was suspended forever (eyes stayed lit but frozen). The older `animations.speak_with_emotion` path didn't hit this because it calls `set_eye_emotion` at each utterance start. Fix is centralized in `leds_head.speak_stop()`: after stopping the mouth it calls `_resume_eye_blink()`, which re-asserts `ACTIVE` (preserving the current eye colour) so blinking resumes — but leaves the eyes off if no colour is set (so `OFF`/sleep stay dark; `off()`/`sleep()` don't route through `speak_stop`). No Arduino change. Tests: `test_head_leds.py` (`test_speak_stop_resumes_eye_blink_when_eyes_have_colour`, `test_speak_stop_leaves_eyes_off_when_no_colour_set`).

- Randomized two-axis room scan (the startup "look around for a person" motion felt robotic — it swept one axis at a time in the same fixed order every boot): the face-acquisition / speaker-gaze search in `intelligence/consciousness.py` no longer cycles a fixed pose list (`down, down_left, down_right, left, right, center`). `_build_speaker_gaze_search_plan()` now generates a fresh randomized pass each time — horizontal lanes spread across the full neck range, each jittered and paired with its own random down-biased pitch, then shuffled — and `_speaker_gaze_search_targets(neck_frac, vert_frac)` (signature changed from a pose string to fractions) resolves each waypoint so **neck AND head pitch move together every beat** (diagonal moves, not one-axis-at-a-time). The pass is stored as `search_plan`/`search_plan_index` on `_speaker_gaze_intent` and re-rolls when exhausted (so a long search keeps varying); `_speaker_gaze_search_label()` makes the human log/`search_pose` label (`"{horiz}_{vert}"`, e.g. `hold_down`, `left_low`). PRESERVE: beat 0 still drops the gaze straight **down without turning** (people are usually seated/below the camera — guarded by `test_unknown_speech_without_face_searches_down_first`), the visor stays open during the search, vertical never pushes past the adaptive rest toward "up", and each pass closes by recentering to neutral/level. Knob: `config.SPEAKER_GAZE_SEARCH_POINTS` (lanes per pass, default 5 → ~7 beats ≈ one pass inside `SPEAKER_GAZE_SEARCH_WINDOW_SECS`); lanes span the full `[-1, 1]` (endpoints included) so the outer beats crane all the way to the neck's configured min/max. `SPEAKER_GAZE_SEARCH_NECK_FRACTION` now defaults to **1.0 (full neck travel)** — that is the intended look (Rex turning edge to edge to find people), the `SERVO_CHANNELS["neck"]` min/max ARE the safe limits and every target is `_clamp_servo`'d to them; dial it down in `.env` only if the full sweep is too much on a given build. `_DOWN_TILT_FRACTION` / `_DOWN_LIFT_FRACTION` still bound the downward pitch. Tests: `tests/test_face_tracking.py` (`test_speaker_gaze_center_search_uses_adaptive_vertical_rest` now calls the `(0.0, 0.0)` resolver; the down-first test checks the label ends in `down`).

- Startup dead-space fix + "look around while loading" (the boot line used to play AFTER all the heavy preloads, with a frozen head and dead silence in between): in `main._run_controller_startup`, the boot line ("Hang on folks while I'm booting up…") and a head scan are now kicked off **before** the slow preloads (Whisper / speaker-ID / Ollama-up-to-30s / animal detector) so they overlap the load instead of following it — matching what the `STARTUP_BOOT_TTS_*` config comment always claimed. Order: after `tts.prewarm()`, start `animations.boot_scan_thread(stop_event)` (daemon) + `_start_startup_boot_tts_thread()`, run the preloads, then join the boot TTS and `stop_event.set()` + join the scan (which recenters the head) — all before sensors/`consciousness.start()`, so face tracking inherits a centered pose and nothing else fights the head during the window. `STARTUP_BOOT_TTS_DELAY_SECS` default dropped **1.5 → 0.3** (it's now just the beat after the startup clip, not a pause in front of all the loading). `animations.boot_scan_thread` sweeps randomized **two-axis** poses (neck across its full `SERVO_CHANNELS["neck"]` range via `move_to`, which clamps to the safe limits, paired with a varied downward pitch) and **yields while `animations._speaking` is set** (same gate wander/breathing/listening-motion use) so the spoken line's own speech gestures own the head, then resumes scanning for the rest of the load. Gated by `config.STARTUP_LOADING_SCAN_ENABLED` (default True) and only started when servos are connected (`SERVOS_ENABLED and servo_ok`). **REVISED (do not regress): the boot line now plays and FINISHES before the preloads, not concurrently** — running the CPU-heavy preloads (esp. Whisper ~2s) while the line played starved the audio thread and glitched/echoed it. Order is now: start scan → start boot TTS → **join the boot TTS** (it plays on an idle CPU, clean) → preloads (the scan keeps the head alive during them; it yields to his speech via `_speaking`, then scans during the loads) → stop+recenter scan. No dead pause (Ollama preload is ~0.1s now, total preloads ~3s, covered by the scan) and no audio glitch. Do NOT move the preloads back to run concurrently with the boot line. The listening **chime moved out of the opening `STARTUP_AUDIO_FILES` burst to the END of startup** (after models load + `interaction.start`, before "online") as a "done loading, listening" cue — `main` plays `LISTENING_CHIME_FILE` there (gated by `PLAY_LISTENING_CHIME`) and calls `speech_queue.mark_startup_chime_played()` so no duplicate chime is inserted before Rex's first reply. Opening burst is now just `light_speed.mp3` → `Roger Control.mp3` → boot line. Test: `test_startup_burst_defers_chime_to_ready_cue`.

- Whisper hallucination repetition filter false-positived on natural repetitive speech (do not regress): `audio.transcription._is_hallucination` discarded "I like lots of composers. I like Beethoven, I like Bach, I like Tchaikovsky, …" — the user's REAL utterance — because the loop check flagged any word whose RAW count exceeded `WHISPER_REPETITION_THRESHOLD` (4), and "I"/"like" appeared 5× in a varied sentence. A real Whisper loop ("you you you you") is ONE word *dominating* the utterance, not a function word recurring across distinct clauses. Fix: flag only when the top word exceeds the count threshold AND makes up at least `config.WHISPER_REPETITION_DOMINANCE` (0.5) of all words. Verified: the composers line + rephrasings pass; "you you you…" and "thank you thank you…" loops still filtered. Tests: `test_repetition_filter_keeps_natural_repetition_but_drops_loops`. (This was the "none of it got transcribed / he hears me worse than several commits ago" symptom — Whisper transcribed it fine; the filter threw it away.)

- Software acoustic echo suppression (`audio/aec.py`) — lets a wake word be heard over Rex's own speech. Live runs proved the wake word is fully MASKED while Rex plays (score ~0 during, 0.99 the instant he stops; no `[wake_diag]` near-misses even ≥0.2 → masked below the 0.30 floor, so the threshold delta alone can't help). The ReSpeaker Lite's hardware AEC is unreachable (playback can't be routed through the ReSpeaker in the robot's wiring, and they want host portability), so AEC is done in software. Approach (chosen deliberately): the mic (ReSpeaker, 16 kHz) and the output device run on INDEPENDENT, drifting clocks, which defeats a sample-locked adaptive filter (NLMS) — so instead of cancelling sample-by-sample we do reference-based **spectral suppression** (magnitude-domain, robust to phase/drift). Flow: `sd_guard._guarded_play` taps every played buffer → `aec.push_reference(audio, sr)` resamples it to 16 kHz (soxr, scipy fallback) and lays it on a monotonic-clock timeline (`_RefTimeline`, clip placed across its real play span); the wake loop calls `aec.process(mic_chunk)` which reads the time-aligned reference, refines the residual lag by cross-correlation every ~0.25s (tracks drift), learns the per-bin echo gain (EMA, FROZEN during double-talk so the user's voice isn't learned as echo), and spectrally subtracts. SAFE BY CONSTRUCTION: `process()` is a pure passthrough whenever Rex isn't playing (no recent reference energy) or on any exception, so it can NEVER degrade normal (Rex-quiet) wake detection — it only touches the during-playback case, which was otherwise fully masked. Gated by `config.AEC_SOFTWARE_ENABLED` (default True); tuning knobs `AEC_OVERSUBTRACTION` / `AEC_SPECTRAL_FLOOR` / `AEC_MAX_DELAY_SECS` / `AEC_DOUBLETALK_RATIO` / `AEC_REF_ACTIVE_RMS` / `AEC_GAIN_EMA` (all `.env`-overridable — AEC needs on-hardware tuning). Diagnostics: periodic `[aec] ERLE=… dB delay=…ms double_talk=…` plus the existing `[wake_diag]` near-miss scores. Verified in tests (`tests/test_aec.py`): ~20 dB suppression of an aligned echo (incl. a 44.1 kHz→16 kHz resampled reference end-to-end), near-end preserved under double-talk, and exact passthrough when disabled / when Rex is quiet. SCOPE: currently cleans only the WAKE-WORD path (the interrupt). VAD/transcription still uses the AEC-by-attenuation path, so fast follow-ups spoken over Rex's tail can still be truncated ("tell me about star trek…" → "star trek…") — routing the cleaned signal into VAD/transcription too (full-duplex barge-in) is the natural next step once the wake path is validated on hardware. NOTE: real-world cancellation depends on how well alignment holds under clock drift; the 20 dB is the aligned-synthetic ideal — watch the live `[aec] ERLE` to see what's actually achieved and tune. **OUTCOME — software AEC DISABLED (`AEC_SOFTWARE_ENABLED=False`, do not re-enable without a new approach):** in the real room it cancelled only **~5 dB** (`[aec] ERLE=4.8 dB ref_rms=0.265` while Rex played loud) — clock drift between the output device and the ReSpeaker mic + reverb defeat host-side cancellation — and WORSE, its distorted residual made Rex's own voice score HIGHER on the wake model (0.36–0.78 vs <0.2 raw), so with the lowered playback threshold he self-triggered and interrupted himself every utterance ("stopped speaking altogether"). The code (`audio/aec.py`, the `sd_guard` reference tap, the wake-loop `aec.process`) is left wired but inert behind the flag. Also disabled the TTS threshold delta (`WAKE_WORD_TTS_PLAYBACK_THRESHOLD_DELTA=0.0`) and re-added the stand-down-during-Rex's-TTS guard in `_detection_loop` (gated by `WAKE_WORD_ALLOW_DURING_TTS`, default False) — necessary because Rex's own "Hey <name>" greeting lines score high on the Hey_rex model and self-trigger; DJ music stays exempt.

- Hardware AEC path (ReSpeaker Lite onboard XU316 — the real fix for wake-while-Rex-speaks): mid-speech barge-in needs the wake word to read a mic with Rex's voice already removed, which host-side software can't do here. The ReSpeaker Lite does AEC on-chip, but: (1) the STOCK USB firmware's AEC does not actually cancel (confirmed on Seeed's forum) — you must DFU-flash the experimental AEC firmware `ffva_ua_v2.0.6_output_proc0_ref0.bin`, which outputs the echo-cancelled audio on **channel 0** and the raw reference on **channel 1**; (2) Rex's audio must play OUT THROUGH the ReSpeaker (the chip uses the USB-output stream it receives as the AEC reference) with the amp/speaker on the ReSpeaker's 3.5mm jack or JST connector. Code support added (all opt-in, inert by default): `config.AUDIO_OUTPUT_DEVICE_INDEX`/`AUDIO_OUTPUT_DEVICE_NAME` → `main._configure_audio_output_device()` sets `sd.default.device` output so all playback routes through the ReSpeaker; `config.AUDIO_AEC_INPUT_CHANNEL` → `audio/stream.py` `_callback` reads ONLY that channel (never mixes — mixing ch0+ch1 would re-add the echo); then set `WAKE_WORD_ALLOW_DURING_TTS=True` so the wake word (now reading the clean channel) may fire during his speech. `.env.example` documents the full 5-step setup. NOTE: unvalidated — depends on the user flashing firmware + wiring the amp to the ReSpeaker; per the forum even the experimental firmware's AEC quality is room-dependent.

- Wake-word multi-fire + head-thrash fixes (a messy live run: "he repeated himself twice", "kept turning his head, rarely locked on", "didn't capture what I said"). Root causes from the log: (1) one spoken "hey rex" lit the openWakeWord model for ~6 frames over 4s and EACH frame re-fired the callback → Rex played the wake-ack 4× (`cache hit c156…/f0f5…` repeated). Fixed with a fire cooldown: `audio/wake_word._detection_loop` now picks the single best model per frame and fires the callback at most once per `config.WAKE_WORD_REFIRE_COOLDOWN_SECS` (1.5s) — `_last_wake_fire_at`. (2) The speech-triggered face-acquisition SEARCH fired even though a known face was locked (`lock=db:1` but `[speaker_gaze] … visible=False search=True`), because a mis-scored voice (speaker-ID 0.65–0.67 < 0.75) is treated as `unknown_voice` and `note_speaker_gaze_intent` only checked for *unknown* visible faces — so Rex abandoned the lock to scan. Now: for a non-`off_camera_unknown` unknown voice, if ANY known face is visible, treat as visible (no search). (3) The search reused the **full-neck-range** startup scan (the user's earlier "full range" request), so when it did fire mid-conversation it swung edge to edge and yanked the camera off the face — cascading into the jumpy face-detection/head-thrash (closed-loop: head moves → face leaves frame → detection jumps → head chases). Now `_build_speaker_gaze_search_plan(reason)` keeps full range ONLY for `startup`/`scan`; mid-conversation searches scale by `config.SPEAKER_GAZE_SEARCH_SPEECH_NECK_SCALE` (0.45). Still open (not yet fixed, likely environmental/architectural): 20–22s turn latency on `conversation.repair` and vision (`query_what_do_you_see`) turns, verbose replies, weak speaker-ID scores, and possible servo-noise-into-mic capture loss while the head thrashes (the head-calming above should reduce it). Tests: full suite green; wake-debounce logic is inline in `_detection_loop` (no isolated test).

- Startup vision false-negative → duplicate chime → starved greeting (ONE root cause, do not regress): a live boot announced "Vision system unavailable" while the camera was fine, played the startup chime TWICE, and never spoke the first-sight greeting it logged as commanded. All three trace to one thing: the C922 via ffmpeg AVFoundation needs ~3s to open + deliver frame 1 (longer when it logs "Configuration of video device failed, falling back to default"), but `main._run_controller_startup` waited only `STARTUP_SENSOR_WARNING_CAMERA_WAIT_SECS=2.5s` (`camera.wait_for_frame`) before declaring vision dead. Fixes: (1) bumped the wait to **6.0s** — `wait_for_frame()` returns the instant a frame arrives, so a larger value only costs time on a truly dead camera, never a healthy one. (2) The bogus "vision unavailable" line was the FIRST speech enqueued, so the speech queue prepended its own listening chime (`_maybe_add_startup_chime_locked`) AND `main` played its end-of-load "ready" chime = two chimes; `mark_startup_chime_played()` ran too late (after the warning enqueue). Now `main` claims the chime EARLY (before the sensor-warning block, gated `not no_audio and PLAY_LISTENING_CHIME`); the end-of-startup call is a now-idempotent re-claim. (3) The greeting wasn't a separate bug — speaking the bogus warning + dup chime tied up Rex's voice ~10s, so by the time the first-sight greeting generated, the user had spoken (state→ACTIVE) and the action_governor correctly suppressed it (`can_proactive_speak_false`); removing the false warning frees the early window so the greeting fires after the 3s first-sight confirm. Do NOT lower `STARTUP_SENSOR_WARNING_CAMERA_WAIT_SECS` back toward 2.5 or move the chime claim after the sensor warning. Tests: `test_startup_burst_defers_chime_to_ready_cue`, `test_mark_startup_chime_played_skips_first_text_chime`.

- Empty/blank transcript drops back to IDLE instead of camping in ACTIVE: a false VAD trigger or fully-filtered audio (Whisper hallucination loop) transcribes to `""` and `_handle_speech_segment` returned at `if not text:` — but left the state ACTIVE, so the session sat dead until the 45s conversation idle timeout. Now an empty transcript that only just flipped Rex to ACTIVE (`from_idle_activation`) sets `State.IDLE` immediately (`final_executed_path="ignored.empty_transcript"`), mirroring the crosstalk path right below it. Mid-conversation blank triggers (NOT `from_idle_activation`) deliberately STAY ACTIVE so a stray noise doesn't end a real exchange. Tests: `test_empty_transcript_from_idle_returns_to_idle`, `test_empty_transcript_mid_conversation_stays_active`.

- Self-speech suppression no longer eats the front of replies to STATEMENTS (do not regress; hardware/room-sensitive — validate on the robot): live run — Rex said "…a glorified warehouse for politics.", the user replied "there's much more to Sacramento than politics" and Whisper got only `'to Sacramento than politics.'`. The front was lost at AUDIO CAPTURE (raw straight from mlx_whisper, not a text filter). Root cause: the post-TTS handoff treated questions and statements asymmetrically — questions got a short tail + no flush + preroll grace (immediate answer survives); STATEMENTS got the full 0.5s mic-attenuation tail + a destructive `stream.flush()` + 0 grace, so a reply that began as Rex finished a statement was wiped. KEY insight: the rolling buffer stores UN-attenuated audio (the ×`AEC_SUPPRESSION_FACTOR` 0.05 hits only the VAD decision in `_chunk_for_vad`, NOT the stored buffer), so the front was physically present — the flush threw it away and the 0-grace capture floor (`_listen_capture_floor_at`, used by `_audio_for_segment`) refused to reach back. Fix gives statements the same responsive treatment: no flush (`POST_SPEECH_FLUSH_AUDIO_BUFFER=False`), short tail (`POST_SPEECH_PLAYBACK_SUPPRESSION_SECS=0.25` — a SCOPED knob; global `POST_PLAYBACK_SUPPRESSION_SECS` stays 0.5 for startup/wake/direct playback), shorter listen delay (`POST_SPEECH_LISTEN_DELAY_SECS` 0.35→0.2), and a 0.12s preroll grace (`POST_TTS_CAPTURE_PREROLL_GRACE_SECS` 0.0→0.12) that reaches back into the raw buffer. BOTH handoff paths changed: `interaction._post_tts_handoff_policy` / `_end_response_sequence_for_text` (+ the two streaming end-sequence spots, now via the shared `_reply_playback_tail_secs(asked_question)` helper) AND `speech_queue._playback_handoff_options`. REMAINING LIMIT: words spoken DEEP over Rex's voice (mid-sentence, not just as he finishes) still can't be recovered in software — reaching that far back would feed Whisper Rex's own voice; needs the hardware AEC path above. If Rex starts self-triggering on his own echo tail after a statement, the 0.25s tail is too short for the room — raise `POST_SPEECH_PLAYBACK_SUPPRESSION_SECS` toward 0.4–0.5 in `.env` (all four knobs are `.env`-overridable). Tests: `PostTtsHandoffPolicyTest` (statement handoff now asserts no-flush + tail preroll), `PostQuestionHandoffStickinessTest::test_statement_outside_sticky_window_uses_normal_delay`.

- Proactive-speech "yield the floor" guard (do not regress; fixes "Rex talked over me right as I started answering"): live run — Rex asked "What's the hardest thing you've had to deal with?", the user paused to think, and idle banter fired and played over the start of their answer; the user then couldn't interrupt (half-duplex). Root cause is NOT a too-short wait timer — it's a **race/lag**. A proactive line is decided on a stale silence timer (`idle_for`, measured from when *Rex* stopped via `_apply_post_tts_handoff` setting `_last_speech_at`), and then the path runs LLM + TTS **inline/blocking** (`_speak_blocking`) for ~1-2s. The interaction VAD loop (`_CHUNK_SECS=0.032`, normally ~31 checks/s) is paused that whole window, so a user who begins talking during generation/TTS-fetch is invisible; by the time audio plays the user is mid-sentence and AEC then masks them. KEY insight: checking VAD just before `_speak_blocking` does NOT fix it — `_speak_blocking` still pays the ~1s ElevenLabs time-to-first-stream after the check, re-opening the gap. So the fix **pre-caches the TTS, THEN re-checks the mic immediately before the (now-instant) playback, and aborts if the user is already talking.** `audio/barge_guard.user_speaking_now()` scans the last `PROACTIVE_SPEECH_YIELD_WINDOW_SECS` (0.6) of the **un-attenuated** rolling buffer with `vad.get_speech_segments`, returns True when ≥ `PROACTIVE_SPEECH_YIELD_MIN_SPEECH_SECS` (0.1) is speech, and **keeps re-checking forward for up to `PROACTIVE_SPEECH_YIELD_POLL_SECS` (0.35)** so a reply that BEGINS in the same beat the line fires is caught, not only one already in progress (a single-instant look-back missed a user who "waited 5s then quickly answered" right as the no-response timer elapsed — the live failure that motivated the poll). It returns early the instant speech is seen, and returns False while `echo_cancel.is_suppressed()` (the buffer would hold Rex's own voice). `interaction._speak_proactive(text, emotion, …)` wraps `tts.ensure_cached` → `user_speaking_now()` → `_speak_blocking` (abort = return False, no speech) and backs every self-initiated interaction-side line: idle banter, interest idle follow-up, low-memory idle question, idle outro, the **no-response recovery quip** (`_arm_no_response_recovery`, a TIMER-thread path — this is what bit in live testing), and the **incomplete-turn prompt** (`_maybe_prompt_incomplete_turn`). `consciousness._speak_async` does the same `ensure_cached` + `user_speaking_now` check before its `priority=0` enqueue (proactive greetings/check-ins/small-talk). On abort the line isn't spoken and the un-attenuated buffer lets the normal turn loop capture the user; the per-path "already said this" / consume bookkeeping (`_idle_banter_count`, `_interest_idle_followups_spoken`, `_low_memory_idle_questions_spoken`, `_idle_outro_spoken`, and the no-response quip's `decline_latest_pending_question`) was moved into the `if completed:` blocks so a yield doesn't consume it — critically, an aborted no-response quip leaves the pending question OPEN so the user's incoming answer still matches it. `audio/vad.py` now serializes all model access with a lock (`_lock`) because the guard runs on the consciousness AND no-response timer threads concurrently with the interaction VAD loop — a `get_speech_segments` reset/inference must not corrupt the streaming `is_speech` state. SCOPE/LIMIT: this is NOT true barge-in — it only catches speech that began **before/at** the moment Rex starts (the common case). Speech that starts **deep into** Rex's playback still hits the half-duplex/AEC wall and needs the hardware AEC path. **Direct replies (the user just spoke) are deliberately NOT guarded** — only self-initiated speech; do not convert reply-path `_speak_blocking` calls (e.g. identity-match prompts, curiosity questions, command responses) to `_speak_proactive` or Rex will fail to answer. Gated by `config.PROACTIVE_SPEECH_YIELD_ENABLED` (default True); hardware/room-sensitive (false-positive yields → raise `PROACTIVE_SPEECH_YIELD_MIN_SPEECH_SECS` or shorten the poll; missed yields → widen the window/poll). Do NOT move the mic-check before the `ensure_cached` pre-cache (that re-opens the TTS lag). Tests: `tests/test_barge_guard.py`; idle-path tests in `test_audio_and_conversation_gating.py` now mock `tts.ensure_cached` + `barge_guard.user_speaking_now`.

- Curious-conversationalist revamp (do not regress; fixes "Rex killed my astrophotography topic with a lame joke and showed no curiosity"). Live failure: Rex asked "what are you obsessed with?", the user said "astrophotography", and Rex replied "Bet you wish you could beam me up for those sweet cosmic selfies" then dropped it — because three layers independently strangled his curiosity. Root causes + fixes (all `tests/test_conversation_revamp.py`):
  - **Short share treated as a throwaway**: a one-word answer naming a passion drove `response_length` → micro/12-words/no-question and `user_energy` → `quiet`/low-appetite. Now `intelligence/conversation_steering.py` defines `INTEREST_SEED_QUESTION_KEYS` (`obsession`/`hobbies`/`favorite_movie`/`travel`/`job`; NOT `favorite_music` — that's the music-offer flow) + `looks_like_interest_seed_answer()`/`seed_from_answer()`; `interaction._maybe_capture_pending_qa` seeds the answer as the active topic; `response_length.classify` gives interest-seed answers short/50w/3s + a follow-up; `user_energy._classify` marks them engaged (depth), not quiet. A one-word answer to "what are you into?" is the HIGH point of the convo, not low energy.
  - **Curiosity rationed backwards**: the question budget counted Rex's own greeting/canned questions, then vetoed his reactive follow-up when the user finally opened up. Now `social_frame.build_frame` lets an EARNED on-thread follow-up (`_explicit_followup_allowed`: interest/answer/identity directives) bypass the budget; the budget still rations NEW-topic/proactive/canned questions. `conversation_agenda` interest + answered-question branches always offer the on-thread follow-up (not budget-gated).
  - **"Fair enough." dead-ack**: `social_frame.govern_response` stripped a question-only reply to empty → `_fallback` "Fair enough." (it deleted Rex's real curiosity twice in the log). Now a pure-question reply is KEPT (`_salvage_pure_question`, except closure/check_alive) and `_fallback` is never the dismissive "Fair enough." (→ "Tell me more."). Statement+question still drops the question (existing behavior preserved). Idle banter with `ask_user=True` now forces `allow_question=True`.
  - **Roast reflex over sincerity**: `social_frame.build_directive` is ENGAGE-FIRST (curiosity leads, roast rides on top) for `interest`/`answer_ack` purposes; ROAST-FIRST stays the default for banter/visual/general turns and for Rex ANSWERING the user (`purpose="answer"`). `conversation_agenda` generic-react no longer forces a punchline onto a plain/sincere statement (the "things are going well → found a way to avoid all problems in the galaxy" non-sequitur).
  - **Curiosity engine (Tier 2)**: `conversation_steering` rotates a per-topic follow-up angle across turns (`_FOLLOWUP_ANGLES`: what got you into it → favorite part → hardest part → best work → what's next → who started you), advanced once per continuing user turn, so Rex digs deeper instead of re-asking the opener.
  - **Topic threading**: `topic_thread` no longer produces garbage labels ("things / are" — added filler stopwords) and an answer to Rex's question retitles the thread to the answer's topic instead of staying stuck.
  - **Repair narrowing**: `repair_moves` — a bare "(No,) I didn't say that" is a content disagreement (normal conversation), NOT a wrong-person identity repair (`_BARE_CONTENT_DENIAL_PAT`; removed `i didn't say that` from `_WRONG_PERSON_PAT`). Explicit identity cues ("wrong person", "that was Jane, not me") still repair.
  - **No vague-memory cold opens**: `consciousness._pick_due_celebration_checkin` only leads a first-sight greeting with a CONCRETE celebration — `_celebration_worth_leading_with` rejects vague inferred affect ("the speaker feels proud of their problem-solving skills") and stale-uninvited events; gated by `config.PRESENCE_CELEBRATION_REQUIRE_CONCRETE`/`PRESENCE_CELEBRATION_LEAD_MAX_AGE_DAYS`. Otherwise the greeting falls through to a normal warm opener.
  - **Comedy alignment**: `comedy_modes.select_mode` uses a curiosity-friendly pool (`dry_ack`/`friendly_roast`/`cantina_color`) on interest turns — never `self_own`/`fake_system_error` derails that ignore the human's topic. Net: the same astrophotography turn now yields engaged, room-to-breathe, question-bearing curiosity even on a spent budget (integration guard: `AstrophotographyTurnEndToEndTest`).

- Turn-taking + routing + dialogue revamp (round 2; do not regress; fixes "Rex interrupts unfinished thoughts, never lets me answer, then misroutes / over-roasts"). Live failures and fixes (all `tests/test_conversation_revamp.py`):
  - **A1 — interrupts a mid-sentence pause**: "well we're currently in" (the user paused before "…galaxy season") got a full reply in ~1s because `turn_completion._INCOMPLETE_END_WORDS` was missing common danglers. Added `in/on/at/of/as/by/up/out/over/a/an/my/our` and widened `_COMPLETE_PREPOSITION_QUESTION_PAT` + `_COMPLETE_EMBEDDED_PREPOSITION_PAT` to those prepositions so genuine questions ("what's it based on") aren't falsely held. A dangling fragment is now held (and merges with the continuation) instead of being answered.
  - **A2 — proactive pile-on**: a turn that ended on a rhetorical "So what you're saying is …?" armed the no-response quip, and Rex stacked an idle-banter question AND "No answer. Bold strategy." within ~4s. `interaction._question_sentence_expects_response` now treats "so what you're saying is/you're telling me/that means/I guess/I take it …?" as rhetorical (no quip). Added a shared `_last_proactive_line_at` (set in `_speak_proactive`, reset in `_begin_user_turn`) + `_proactive_line_recently_fired()` so the no-response quip defers to a just-played proactive line (`PROACTIVE_LINE_MIN_GAP_SECS=6`). `CONVERSATION_NO_RESPONSE_QUIP_SECS` 7→12 and the quips are now gentle, not accusatory.
  - **A3 — no time to answer**: `_arm_no_response_recovery` sets `_floor_held_until = now + POST_QUESTION_FLOOR_HOLD_SECS` (10s, only on real questions); `_maybe_idle_banter` holds the floor until then, so after Rex asks a question the user gets a real window before idle banter re-engages. (Defaults to 0 so it never affects non-question turns or tests.) The deeper "can't be heard while Rex talks" is the documented half-duplex/AEC limit — mitigated by talking less, fully fixed only by the ReSpeaker hardware-AEC path.
  - **B1 — "It's 8:33 PM." misroute**: "You didn't give me any time to answer" matched `intent_classifier._TIME_QUERY_RE` ("give me … time"). Tightened it to real clock queries only ("what time", "the/current time", "time is it/now", "o'clock") — "give me time to answer"/"any time" no longer match.
  - **B2 — blocked repair stayed conversational**: when the LLM router judged a turn `conversation.repair` but it was blocked for missing evidence, it fell through to the keyword intent classifier. `interaction._intent_execution_block_reason` now takes `router_action` and blocks deterministic data intents (`router_classified_repair`) when the router already called it a repair — a repair is never a clock/date query.
  - **B3 — pacing complaints**: `repair_moves._INTERRUPT_PAT` now catches "you didn't give me time to answer / didn't let me finish / wouldn't let me respond" → the existing interruption repair ("You're right. I cut in. Go ahead, I'm listening."), not a misroute.
  - **C1 — relentless roasting** ("he could have just said 'good choice'"): `social_frame.build_directive` "normal" roast rule changed ROAST-FIRST → **ROAST-LEAN** — still sharp/SPECIFIC when it roasts, but no longer a mandatory jab every single turn (a plain genuine reaction is explicitly allowed). ENGAGE-FIRST (interest/answer_ack) likewise now permits a plain "good choice." Keeps the roast personality; removes the every-turn mandate. Tests updated: `test_normal_roast_directive_is_roast_lean_and_specific`.
  - **C2 — repetitive openers** ("Glad…"/"Glad…", "Ah,"/"Ah,"): `comedy_modes.strip_banned_opener` deterministically drops "Ah,"/"Oh,"/"Well, well, well," (single "Well," left alone) in `polish_response`/`polish_stream_sentence` (non-straight turns only, so sympathy isn't mangled). `note_spoken_line` (called from `interaction._register_rex_utterance`, so it covers streaming + non-streaming) records each line's opener; `build_directive` adds an "Opening variety: don't open with {recent}" note.

- Repeat / hallucination / truncation / joke-replay fixes (round 3; do not regress; all `tests/test_conversation_revamp.py` + `test_performance_output.py`):
  - **P1a — Rex repeated his own question over the user's answer** (regression from the round-1 `kept_question_over_dead_ack` salvage). Live: Rex said "A solo project, huh?", the user said "Absolutely", and Rex re-asked the identical "A solo project, huh?" (governance salvaged the same question; the TTS cache replayed the same audio). Fix: `comedy_modes.note_spoken_line` now also records the full `_LAST_SPOKEN_LINE` (`last_spoken_line()`); `social_frame._salvage_pure_question` skips a candidate that `_is_near_repeat`s Rex's last line (so it salvages a DIFFERENT follow-up instead), and `govern_response` has a final `deduped_repeat` backstop that replaces any near-repeat of the last line with a fallback. Closure acks may still repeat.
  - **P1b — Whisper subtitle/credit hallucinations** ("Subs by www.zeoranger.co.uk", "Thanks for watching", "like and subscribe", stray URLs) now filtered by `audio/transcription._SUBTITLE_HALLUCINATION_RE` in `_is_hallucination`. This ALSO fixes the missing departure reaction: the hallucination was being attributed to the (absent) user, refreshing his "still engaged" timer so `_step_presence_tracking` never fired a "where'd you go?"; filtered, the timer runs and departure can confirm. ("I need to subscribe to Netflix" / a real Apple-TV mention still survive — only the credit/URL forms are dropped.)
  - **P2a — Rex cut himself off mid-sentence** ("Glad to", "What's the deal"). The streamed-reply tail (leftover after the last sentence boundary) was spoken even when the model was cut mid-sentence. `interaction._tail_is_speakable` now speaks the final tail only if it ends in terminal punctuation; an unpunctuated remainder is dropped (logged "dropped incomplete stream tail"). A complete final sentence (ends in `.!?…`) is unaffected.
  - **P2b — the joke played/logged 3×** (setup, punchline, then the whole joke again). `speech_queue.enqueue` logs each spoken line, so `performance_output`'s setup+punchline were each logged AND the main loop logged the full joke. Fix mirrors the streaming path: `_speak_blocking` gained `log_text` (passthrough to the queue) and `performance_output` speaks its pieces with `log_text=False`, so the caller logs the whole performance once. Joke now appears once in the conversation log, delivered as setup+punchline.
  - **P3a — stale steering / garbage topic labels.** Steering stayed on "watching Apple TV Plus" while the convo moved to droid-building. `conversation_steering` now also detects "I'm/I am building|making|working on|… X" as an interest (so the active topic UPDATES to the droid; "I'm trying to make him funny" with "trying to" is skipped), and the active-interest TTL dropped 15→8 min. `topic_thread` stoplist gained more filler ("watching", "trying", "personally", "currently", "tonight", …) so labels like "watching / apple" don't form.
  - **P3b — don't needle a reassurance.** "I'm not sad, it's okay" got roasted for "repressing feelings". `conversation_agenda._looks_like_reassurance` adds a primary-purpose branch: take a reassurance/de-escalation at face value, respond warmly/lightly, and explicitly do NOT roast/needle or imply they're repressing. (Same directive-only pattern as the health-resolved branch.)

- Subject pivot — change the channel when a topic isn't landing (do not regress; `tests/test_conversation_revamp.py::SubjectPivotTest`). Previously the steering engine only ever DEEPENED the active interest (rotating follow-up angles), so it kept probing a subject even when the user had checked out. Now `conversation_steering` tracks per-topic engagement: a bare low-content reply ("yeah", "sure", "I guess", "not really" — `_GENERIC_REPLY_RE`/`_looks_disengaged`, whole-utterance match so a real short answer like "mostly nebulae" doesn't count) increments a `misses` counter on the active topic; a substantive reply resets it. After `_PIVOT_AFTER_MISSES` (2) in a row, `SteeringContext.mode` flips `deepen`→`pivot`: `_pivot_directive_for` tells Rex to STOP probing the stalled subject and swing to a RELATED subject (adjacent angle / sibling hobby / the wider `_category_for_topic` space) or open a new one, and the dead topic is dropped from `_active` so it can't re-anchor. The agenda steering branch (now reads the `SteeringContext` via `note_user_turn`, not just the directive string) appends a pivot purpose AND offers a concrete fresh `_next_useful_question` (e.g. "What kind of music are you into?"); the pivot's question is phrased as a "natural follow-up" so it's allowed even on a spent question budget (re-engagement, not interview spam). Knobs: `_PIVOT_AFTER_MISSES`, `_TTL_SECS`. Engaged conversations are unaffected (misses stay 0 → always `deepen`).

- Conversation arc memory (Bet 1 — running summary + callbacks; do not regress; `tests/test_conversation_arc.py`). The first structural step away from the stateless engine: a short **running summary of the live conversation** maintained by a cheap LLM call and fed back into the system prompt, so Rex can SEE what he already asked/roasted (stop repeating himself) and can CALL BACK to an earlier thread ("did you fix the droid's eyes?"). **Backend is configurable (`config.CONVERSATION_ARC_BACKEND`):** `"openai"` (DEFAULT) uses `gpt-4o-mini` via the existing OpenAI client with a rich 5-field schema (`Topics`/`Shared`/`Mood`/`Landed-flopped`/`Open threads`); `"local"` uses the `qwen2.5:1.5b` sidecar with a reduced 3-field factual schema (`Topics`/`Shared`/`Open threads`). The cloud default was chosen deliberately: the refresh is off the speech path (its latency/cost never touch the conversation), Rex's replies already require OpenAI (no NEW dependency), it costs ~$0.0002/turn, and — decisively — the 1.5B model could NOT do the affective fields (see LIVE-TUNED below) whereas gpt-4o-mini does them accurately. This first cut is **purely additive** — none of the deterministic anti-repetition (comedy opener stripping, follow-up angle rotation, `social_frame._is_near_repeat`, the P1a no-repeat-question salvage) was removed; the arc is meant to let us delete those later, once it proves out live. Design choices (deliberate): (1) **Folded into `intelligence/topic_thread.py`**, not a parallel module — that module already tracks "what this conversation is about", and the arc shares its session lifecycle (`clear()` wipes both). The summary lives as MODULE-level state (`_arc_summary` etc.), NOT on the `TopicThread` dataclass, because it must survive topic switches within a session (`note_user_turn` replaces `_current` on a new thread). `snapshot()` gained an `arc_summary` key; its existing `label`/`unresolved_question` keys (read by 3 consumers at `interaction.py:4559/11507/12244`) are untouched. (2) **Off the speech path.** `note_user_turn` (already called in the turn loop — so ZERO new wiring in `interaction.py`) calls `_trigger_arc_refresh()`, which marks dirty and ensures ONE coalesced daemon worker is summarizing. The worker re-derives a FRESH summary from the most recent `CONVERSATION_ARC_CONTEXT_LINES` (12) lines of the in-memory session transcript (`memory/conversations.py` `get_session_transcript`) via a single summary call to the configured backend (`_arc_generate` → `llm.summarize_conversation_arc` for openai, else `local_llm.generate`), and commits under `_arc_lock`. The prompt assembler reads the stored summary INSTANTLY (`build_arc_directive()`); the refresh never blocks a turn or TTFS — the summary is current "as of the previous completed exchange", which is exactly what's needed for not-repeating + callbacks. (3) **Injected via `llm.assemble_system_prompt` (section 6b, right after "Session so far"), NOT the agenda directive** — on purpose: the agenda string is regex-reparsed by `social_frame` (`_purpose_from`/`_EXPLICIT_FOLLOWUP_PAT`), and free-text summary lines (which can contain "?") would trip it; the system prompt is downstream of all governors and safe. The directive text carries the steer: lean on it, don't re-ask/reuse, you MAY call back to an open thread when natural, but NEVER force a callback or recite it. (4) **Graceful degradation is mandatory** — `_arc_refresh_core()` NEVER raises; on disabled / no-new-lines / local-LLM-down/slow / empty-output it returns False and the previous summary is retained. A cursor-commit guard discards a summary if `clear()` reset the arc mid-generate; a shrunken transcript (session reset) resets the cursor and re-summarizes from scratch. Gating is two-layer: `_arc_backend_available()` is the real gate (`config.CONVERSATION_ARC_ENABLED` default **True** kill switch AND the chosen backend usable), wrapped by `_arc_enabled()`, which ALSO **fails safe under a test runner**: `_under_test_runner()` returns True when invoked via the unittest/pytest ENTRY POINT (`sys.argv[0]`=='python -m unittest', or `PYTEST_CURRENT_TEST` set), so `_arc_enabled()` is False there unless `DJR3X_ARC_TEST_OPT_IN` is set. It is keyed on the entry point, NOT `"unittest" in sys.modules`, so an incidental `unittest` import by a dependency can't disable the arc on the robot (which runs `python main.py`). **DO NOT remove that test gate:** the suite carries a live `sk-` OpenAI key in `apikeys.py`, and the arc fires from deep inside `note_user_turn` (not a mocked entry point), so without the gate the openai backend would make real cloud calls during `unittest discover`. The trigger also only spawns when there is genuinely new transcript material, so the test suite makes ZERO real arc calls (every `[arc]` log in a test run is a mocked 0.00s call). Knobs: `CONVERSATION_ARC_ENABLED` (kill switch), `CONVERSATION_ARC_BACKEND` (`openai`|`local`), `CONVERSATION_ARC_OPENAI_MODEL` (`gpt-4o-mini`), `CONVERSATION_ARC_MAX_TOKENS` (200), `CONVERSATION_ARC_TIMEOUT_SECS` (8.0), `CONVERSATION_ARC_CONTEXT_LINES` (12). **LIVE-TUNED — the `qwen2.5:1.5b` constraints that drove the openai default (these still apply when `backend=local`; do not regress the hard-won prompt shape — see `intelligence/topic_thread.py` `_build_arc_prompt`/`_arc_output_ok`/`_sanitize_summary`):** the first live run (on the local 1.5B) FROZE — the summary stuck on turn 1's "I don't want to talk about that" and never evolved, and a speaker name ("Bret Benziger") leaked into Topics. Root causes + fixes: (a) **echo/freeze** — feeding the prior summary back ("Running memory so far: … rewrite it") made the 1.5B model echo it verbatim → FIX: re-derive FRESH from the transcript window each refresh; never feed the prior summary. (b) **name pollution** — `_render_transcript_lines` now normalizes speakers to roles (`User`/`Rex`) so a person's name can't land in a field. (c) **the 1.5B model can't judge affect** — it called declined back-pain "Landed" and reported Rex's sarcasm as the user's mood → schema cut from 5 affective fields to **3 factual ones** (`Topics`/`Shared`/`Open threads`); affect already lives in `empathy`/`user_energy`. (d) **format stability** — `temperature=0.0` + SHORT single-word labels + conversation-first + a rigid "EXACTLY these three labelled lines and nothing else" instruction + **NO trailing blank labels** (ending the prompt with `Topics:\n…` turned it into a completion task → it echoed the transcript); verbose multi-word labels make it rename them and balloon into bullets — keep labels terse. (e) **runaway repetition** — a long window dragged repetitive earlier lines in and the model looped `motivation, motivation, …` to fill the budget, and `repeat_penalty` made it WORSE (markdown, label drift) → FIX: small window (12) + two safety nets in `_arc_refresh_core`: `_arc_output_ok()` REJECTS empty / transcript-echo (`^User:`/`^Rex:`) / degenerate single-token loops (keeping the prior good summary), and `_sanitize_summary()` strips markdown and dedups/caps each comma list. Validated end-to-end against live qwen: accurate factual summaries that evolve, no freeze/loop/name-leak. The **openai backend (default) was then validated the same way** on the same replayed conversation: gpt-4o-mini produced accurate, evolving RICH summaries (correct `Mood`, e.g. "evasive"→"playful, amused" as the user warmed up; real `Landed/flopped`, e.g. "engaged with humor about motivation; dodged discussion on back pain") exactly where the 1.5B had frozen — confirming the affective fields belong on the cloud model. The `[arc] summary updated …` INFO line includes the summary text (newlines→`|`) for live inspection; `config.LOG_SYSTEM_PROMPT` (default True, verbose — flip off when done) dumps the full assembled prompt so injection is confirmable. **CROSS-SESSION PERSISTENCE (done):** on session end, a 1:1 session whose transcript fits the arc window persists the arc itself as the per-person `conversations` row (`interaction.py` session-end loop → `topic_thread.arc_persistence_fields()` → `save_conversation`), reusing the already-computed arc and SKIPPING the redundant `generate_session_summary` OpenAI call (Mood→emotion_tone, Topics→topics); longer or multi-person sessions fall back to `generate_session_summary` (full-transcript) so nothing is lost — the arc is recency-windowed (last `CONVERSATION_ARC_CONTEXT_LINES`). The existing `get_last_conversation` injection (`llm.py:434`, "Last conversation: …") surfaces it next session, so "last time you were chasing the Whirlpool galaxy" now rides the richer arc. **ACT-ON-SIGNAL (done):** when the arc's Mood line reads flat (`topic_thread.arc_reads_flat()` — disengaged/bored/disappointed/evasive/…), `social_frame._roast_level()` eases a would-be `normal` roast to `light` so Rex stops needling a flagging room; ADDITIVE (the care/affect `none` branches and the engaged-turn default are untouched), keyed on Mood (not Landed/flopped, which mentions a flop most turns), gated by `config.ARC_EASES_ROAST_ON_FLOP`. "Change the channel" is already handled by the `conversation_steering` pivot. STILL DEFERRED to fast-follows: a `friendship_patterns` callback selector, and deleting the deterministic anti-repetition hacks (only once the arc proves out live). LIVE-VALIDATE on the robot (watch the `[arc] summary updated …` log against `[latency]`/`[ttfs]`); the refresh cadence is once per user turn (alternative trigger: `note_assistant_turn`). Tests cover fold/update, the callback+anti-repeat guard text, retain-on-error, empty-output retain, transcript-echo rejection, runaway-loop rejection, markdown/dedup sanitize, speaker→role normalization, kill switch (flag + LLM-unavailable), cursor bookkeeping, transcript-reset re-summarize, `snapshot()` back-compat, a real background-thread trigger, arc persistence-fields parsing, arc-reads-flat mood detection, and the roast normal→light easing (additive + kill switch + care-cases untouched).

- TurnPlan — typed `conversation_agenda`→`social_frame` handoff (Bet 2; do not regress; `tests/test_turn_plan.py`). Kills the rework's "core problem #1": the agenda emitted a prose directive and `social_frame` regex-reparsed it (`_purpose_from` / `_EXPLICIT_FOLLOWUP_PAT` / `_ASK_ALLOWED_PAT`) to re-derive decisions the agenda had ALREADY made — so every wording tweak risked breaking the next layer's regex. New `intelligence/turn_plan.py` `TurnPlan` dataclass carries those decisions losslessly. `conversation_agenda.build_turn_plan()` is the renamed body of the old `build_turn_directive` (each `return "\n".join(lines)` → `return _finish(plan, lines)`, with an inline `plan.purpose = …` set on the branches it definitively classifies); it populates `plan.purpose` (closure / interest / answer_ack / answer) and renders `plan.directive` (the prompt text). `build_turn_directive()` is now a thin BACK-COMPAT wrapper (`return build_turn_plan(...).directive`), so the many string-asserting agenda tests are untouched. `social_frame.build_frame(..., turn_plan=None)` reads `turn_plan.purpose` when set, else falls back to `_purpose_from` — so passing NO plan (every existing test + any other caller) keeps the exact old behavior. `interaction.py` (the one live caller, ~7230) now calls `build_turn_plan`, uses `.directive` for the prompt, and passes `turn_plan=plan` to `build_frame`, so the LIVE path uses the structured purpose (no regex). INCREMENTAL + ADDITIVE by design — the regex stays as the fallback so nothing breaks; the safety property is the EQUIVALENCE TEST (`PurposeEquivalenceTest`): build_frame must derive the SAME purpose with the plan as without it, for every branch. That equivalence is what makes the live swap behavior-preserving (and why the full suite stayed green at 791). DONE (slices 1–3): the ENTIRE agenda→social_frame question handoff is now structured on the LIVE path — `purpose` (slice 1), `explicit_followup` (slice 2), and `ask_allowed` / `hard_no_question` / `urgent_identity` / `fresh_interest_followup` (slice 3). The signals are an Optional TRI-STATE (None = "agenda didn't decide → regex fallback"). `build_turn_plan` populates EVERY signal: `purpose`/`explicit_followup` by branch KNOWLEDGE (inline `plan.x = …`), and the rest via `_populate_signals()` → `social_frame.derive_signals(directive, purpose)` — the regex run ONCE by the agenda on its OWN rendered directive (not a cross-layer reparse). `build_frame` reads each signal from the plan when set; its regex (`_sig(...)` lambdas / `_purpose_from`) runs ONLY for no-plan callers, so the live `build_frame` no longer reparses the agenda's prose. Equivalence is GUARANTEED BY CONSTRUCTION — `derive_signals` and `build_frame`'s fallback use the SAME patterns — and verified by `FullEquivalenceTest` (`build_turn_plan → build_frame(plan)` == `build_frame(regex)` for purpose AND allow_question across generic / plan-statement / question / reassurance turns) plus `Purpose`/`AllowQuestionEquivalenceTest`. The "set by intent" trap (avoided): `_HARD_NO_QUESTION_PAT` has quirks (it matches "do not ask **another** question" but NOT "do not ask **a new** question"), so the agenda derives the signals via the SAME regex rather than hand-guessing per branch — that's what keeps it behavior-preserving. NOTE: `purpose` for GENERIC turns still flows through `_purpose_from` on the live path — but that legitimately falls through to the user-energy mode (social_frame's domain), NOT the agenda's prose; it is the energy fallback, not the fragility (which is gone). REMAINING (optional, deferred BY CHOICE — patterns KEPT as the no-plan fallback): literally deleting `_purpose_from` / `_EXPLICIT_FOLLOWUP_PAT` / `_ASK_ALLOWED_PAT` / `_HARD_NO_QUESTION_PAT` would require removing the fallback and rewriting the ~5 pinned string-based social_frame tests (`AstrophotographyTurnEndToEndTest`, `EarnedFollowupBypassesBudgetTest`, the `SubjectPivotTest` pivot case) onto the plan API — a clean isolated follow-up. **Do NOT delete the patterns until that test migration is done.**

- Relationship-tone tracking (smaller win; do not regress; `tests/test_relationship_tone.py`). Rex's warmth/edge now tracks the RELATIONSHIP instead of flip-flopping per turn: `llm._relationship_tone_rule(person, name)` maps the person's `warmth_score` / `antagonism_score` / `trust_score` (`memory/people.py`, each 0.0–1.0; defaults warmth=0.0, antagonism=0.0, trust=0.5) into a persistent tone line woven into `assemble_system_prompt`'s per-person block — **affectionate ribbing** for established warm friends (`warmth >= 0.5`, plus a trust clause when `trust >= 0.6`), **sharper sparring** for people who needle Rex (`antagonism >= 0.4 and antagonism >= warmth`), and **"" (no line)** for neutral/new people. Calibrated to the 0.0/0.0/0.5 defaults so a brand-new person reads neutral and the common case is unaffected (scores start at baseline and only grow with interaction). ADDITIVE + TONE-ONLY: it never relaxes the empathy / boundary / family-safe / `_roast_level` gates (those decide WHETHER to roast; this only colors HOW, when a roast is already allowed). `_roast_level` was deliberately NOT modified — its none/light/normal levels are about care/affect, whereas affectionate-vs-sharp tone belongs in the prompt. Gated by `config.RELATIONSHIP_TONE_ENABLED` (default True, kill switch). Tests: warm / antagonist / neutral, trust clause on/off, sparring (both scores elevated → antagonism wins), malformed-score safety, kill switch.

- Offline conversational-quality replay harness (do not regress; `tests/test_conversation_replay.py` + `tests/fixtures/conversation_replays.json`). The first conversational eval that does NOT require the robot: it replays scenarios through the DETERMINISTIC stack (`conversation_agenda.build_turn_plan` → `social_frame.build_frame` → `social_frame.govern_response`) and asserts STRUCTURAL properties (purpose, allow_question, allow_roast, agenda-directive substrings, and on a supplied candidate reply: governed includes/excludes, ≤N questions, governance notes). **No LLM is called** — only the routing/governance decisions around the creative reply are tested — so it runs in the suite with zero network/cost (the whole point: validate conversational behavior without the robot; the "A solo project" repeat shipped because there was no such eval). DATA-DRIVEN: add a regression by dropping a scenario into the JSON corpus — no Python. The engine (`run_scenario` / `assert_scenario`, in the test file — extractable if reused) neutralizes world/memory/budget/empathy via the standard pipeline mocks and resets the session modules per scenario. Seed corpus covers: interest follow-up survives a spent budget, answered-question ack + earned follow-up, reassurance-not-needled, no-repeat-of-Rex's-last-line, statement-plus-question drops the unearned question, and the one-question cap. Complements the existing ROUTING replay (`tests/fixtures/misroute_replays.json` via `test_dialogue_act.py`). NOTE: it covers the deterministic decisions, NOT the LLM's creative text — and it's the natural tool to de-risk the deferred TurnPlan pattern-deletion (task: expand the corpus over the affected branches, then assert outcomes are unchanged across the deletion).

- "What's worth bringing up" cold-open ranker (smaller win; do not regress; `tests/test_celebration_ranker.py`). The startup celebration cold-open used to lead with the FIRST gate-passing candidate (most-recent-that-passes). Now `consciousness._pick_due_celebration_checkin` RANKS the gate-passing candidates via `_celebration_lead_score` = did-they-invite-it (dominant) × recency × concreteness, and leads with the BEST one. Concreteness: a `_CONCRETE_MILESTONE_RE` cue (won / championship / new job / graduated / award / …) → 1.0; otherwise a weak length proxy capped at 0.6, so a wordy-but-vague line ("had a decent day at the office") never outscores a real milestone ("won the championship") — that was a bug the ranker's own test caught. Recency decays with `PRESENCE_CELEBRATION_RECENCY_HALFLIFE_DAYS` (14). The GATE (`_celebration_worth_leading_with`) is UNCHANGED — still covered by `CelebrationColdOpenGateTest` — this only re-orders among the already-worthy. Weights `PRESENCE_CELEBRATION_W_INVITED` (1.0) / `_W_RECENCY` (0.6) / `_W_CONCRETE` (0.3) make "they told Rex themselves" the strongest signal (an invited older milestone beats a slightly-more-recent uninvited one). Kill switch `PRESENCE_CELEBRATION_RANK_ENABLED` (default True) restores the old first-worthy pick. SCOPE: ranks emotional-event celebrations (the named "clumsy cold-open"); extending the same recency×concreteness×invited score across `facts`/`interests` is the natural follow-up. Tests: invited>uninvited, recent>old, milestone>borderline, invited-bonus>recency-edge, pick selects best regardless of list order, kill switch restores first-worthy, no-worthy→None, non-int person→None.

- Turn classifier (Bet 3) — SHELVED / dormant (do NOT regress to on-path; `intelligence/turn_classifier.py`, `tests/test_turn_classifier.py`). A single structured `qwen2.5:1.5b` call returning `{topic, engagement, intent, sentiment, wants_pivot, addressee}` — built, tested, and left INERT behind `config.CONVERSATION_TURN_CLASSIFIER_ENABLED` (**default False**). Validated against the real model: it is **SLOWER AND WORSE** than the existing deterministic heuristics on the high-value fields — `engagement` defaulted to "engaged" even for flat replies ("pretty much", "yeah whatever"), and it MISSED an explicit pivot request ("can we talk about something else") — while adding ~1s of ON-PATH latency per turn (it must complete before the reply is built, so the latency can't be hidden behind the reply call). Any on-path classifier, even the fastest cloud model (GPT-5.4-nano, ~0.57s TTFT → ~1s full), hurts time-to-first-speech, which the user explicitly prioritizes ("latency kills engagement"). The deterministic heuristics (`conversation_steering._looks_disengaged`, `user_energy._classify`) already read engagement/pivot well and instantly, and the conversation arc already supplies a clean topic label — so the classifier is redundant where it works and harmful where it doesn't. **Do NOT enable it on the live turn path.** If ever revisited, run it OFF-path (background, lagged like the arc) or fold it into an existing call. Its unit tests mock the model, so the suite makes no Ollama calls; `enabled()` is gated on the flag + `local_llm.enabled()`.
- Rex persistent POV — "leads with substance" current preoccupation (smaller win / north-star; do not regress; `intelligence/rex_pov.py`, `tests/test_rex_pov.py`). Fixes the rework's "Rex interviews instead of volunteering" — the loop was react→roast→question. Rex now carries ONE **current preoccupation** (a concrete thing he's chewing on) that PERSISTS across turns and is (re)selected with a light HYBRID context bias, so he VOLUNTEERS his own in-character substance instead of only reacting. DETERMINISTIC, no LLM call (the shelved-classifier latency lesson stands — see Turn classifier entry). New module `intelligence/rex_pov.py` holds session-scoped state under a lock (read from concurrent reply/proactive threads). Seeds are authored in `config.REX_POV_SEEDS` (`{id, pov, fits}`; `pov` is 2nd-person, dropped into "Your current preoccupation: {pov}"). SELECTION (`_choose`): score each seed by how many of its `fits` tags match the current CONTEXT SIGNATURE (`people`/`quiet` from world_state visible people; `flat` from `topic_thread.arc_reads_flat()`; `any` never disqualifies), exclude used+current (within-session anti-repeat; resets once the pool is exhausted), deterministic rotation tie-break keyed on the hold clock (no `random()` → reproducible in tests). HOLDING POLICY (the "carries" win): clocked on transcript length (`memory/conversations.get_session_transcript`, ~2 lines/exchange) — held < `REX_POV_MIN_HOLD_EXCHANGES` (4) → KEEP (don't thrash); held ≥ MIN and (context signature changed materially OR held ≥ `REX_POV_MAX_HOLD_EXCHANGES` (14)) → RE-SELECT (this IS the hybrid "evolves with context", no LLM). SURFACES (TWO, lean by design — avoids the proactive pile-on): (1) `llm.assemble_system_prompt` injects `current_pov_directive()` as §6c (downstream of the agenda/social-frame governors, alongside the arc), and because EVERY proactive/idle path calls `llm.get_response` (→ `stream_response` → `assemble_system_prompt`), that single injection colors normal replies AND idle/proactive lines; (2) `interaction._maybe_idle_banter`'s odd ("volunteer something of your own") attempt reads `active_pov_text()` so idle volunteering is the SAME specific preoccupation, not a generic improvised take (falls back to the generic directive when POV off/empty). Session-scoped: `rex_pov.clear()` is in the session-reset bundle (`interaction.py`, next to `topic_thread.clear()`). CONTENT CONSTRAINT (hard user ask; do not regress): seeds are venue-NEUTRAL — Rex usually isn't in a cantina, so NO "cantina" (`test_no_cantina_in_shipped_pool` asserts it across the shipped pool) or other location clichés. GATING: `config.REX_POV_ENABLED` (default True) — off → `current_pov_directive()`/`active_pov_text()` return "" and the idle path uses the old generic text. Unlike the arc it makes NO network call, so it runs directly under the suite (no test-runner cloud gate needed). Related: `intelligence/rex_preferences.py` is the STATIC reactive tastes; this is the dynamic carrying counterpart. Knobs: `REX_POV_ENABLED`, `REX_POV_SEEDS`, `REX_POV_MIN_HOLD_EXCHANGES` (4), `REX_POV_MAX_HOLD_EXCHANGES` (14). FAST-FOLLOWS (deferred BY CHOICE): cross-session persistence (resume/evolve a preoccupation across visits), feeding `_do_private_thought`/`_do_aspiration` from the POV, an LLM-evolved POV. LIVE-VALIDATE on the robot via `--gui` (payoff is qualitative — "less exhausting / leads with substance"; `config.LOG_SYSTEM_PROMPT` dumps the prompt so the §6c "Your current preoccupation:" line is confirmable, and `[rex_pov] selected …` logs each (re)selection). Tests cover: carries within min-hold, re-select after max-hold, re-select on context change (incl. flat-flip), min-hold-wins-over-context-change, hybrid bias (people vs quiet), anti-repeat (no-consecutive + full-cycle), `clear()` reset, kill switch, empty-pool safety, directive content, `active_pov_text` matches active seed, and the shipped-pool guards (well-formed, unique ids, NO cantina).
- Roast rebalance — curious-first, not roast-first (do not regress; `config.py` `REX_CORE_PROMPT` + `PERSONALITY_DEFAULTS`, and the live `personality_settings` DB). A live run (2026-06-03, `logs/conversation.log`) was exhausting: Rex roasted EVERY turn and overrode his own softening governors — he needled a sensitive boundary (back pain) the agenda had explicitly told him to "acknowledge & let drop / light tap", **INVENTED a "half-finished drink in your hand"** with NO visual-data source and **DOUBLED DOWN** when denied, and roasted a sincere share ("your love for astrophotography is only rivaled by your love for clouds"). ROOT CAUSE: the character spine was cranked to roast-dominate — dials `roast_intensity=90`/`sarcasm=80`/`sentimentality=35` PLUS `REX_CORE_PROMPT` framing ("snarky… **roast-first**… **constitutionally incapable of letting anything slide**… This is **comedy first. Lead with the bit**… being agreeable/pleasant **is the failure mode**"). The per-turn governors only ADVISE; the core prompt + dials WIN, so the softer guidance was ignored. FIX (user chose the full rebalance): (1) **DIALS lowered** — `roast_intensity` 90→55, `sarcasm` 80→60, `sentimentality` 35→50 — in BOTH `config.PERSONALITY_DEFAULTS` (first-run baseline) AND the live `personality_settings` DB (so the running robot reflects it without a reset; `agreeability` left at the DB's 70). (2) **`REX_CORE_PROMPT` reframed** roast-first/comedy-first → CURIOUS-first, roast-CAPABLE ("Lead with something real… A roast is one option, not the default, and not every turn needs one"), while explicitly keeping his edge ("not a bland, agreeable yes-droid… a curious conversationalist with a sharp tongue"). (3) **HARD GUARDRAILS** added to the core prompt (highest authority, because the governors get overridden): when someone is sincere / sets a boundary / steers away → DROP the bit, get curious or let it go (sincerity & boundaries are NEVER the target); and "only react to what is actually there — never invent physical details (what someone is holding/wearing/doing) to set up a joke; if corrected, drop it instantly, never double down" (the drink hallucination). (4) **De-cantina'd** per the user's standing aversion (see [[rex-no-cantina-overuse]] + the Rex persistent POV entry): dropped "cantina energy" from the loves-list and the reflexive "reference Star Wars naturally — Batuu, the cantina…" instruction → "reference your world only when it genuinely fits; don't reach for Jedi/Force/galaxy one-liners as a reflex" (the bad run was full of Jedi/Force/Whirlpool clichés). Backstory (the Oga's Cantina ORIGIN) and the concision rules are UNCHANGED. This also reframes the persistent-POV experiment: `rex_pov` was mechanically perfect that run (correct selection/rotation/injection, verified in `logs/djr3x.log`) but NEVER surfaced — the roast reflex crowded it out — confirming you can't make Rex "less exhausting" by ADDING instructions; the roast balance itself is the lever. REVERSIBILITY: dials are DB-backed (dashboard sliders, or re-run the UPDATE); the prompt is plain config. RE-TUNE LIVE: too soft → raise the dials / restore more edge; still mean → lower further. No unit test pins the prompt wording or the dial values (the separate `"Drop roast-first mode completely"` sensitive-mode directive is untouched), so the suite stays green; the felt change needs a live `--gui` pass.
- Memory-followup cadence clamp — stop the proactive event interrogation (do not regress; `intelligence/interaction.py` `_post_response` / `_memory_followup_cadence_allows` / `_note_memory_followup_fired`, `config.FOLLOWUP_*`, `tests/test_followup_resolution.py`). The live `--gui` run that VALIDATED the Roast rebalance (2026-06-04, `logs/conversation.log`) exposed the NEXT layer: with the roast dialed back, Rex was no longer exhausting via *roasting* but via *interrogating* — he ran down a checklist of remembered events one per turn (greeting "how's your back" → "how was your trip to Disneyland?" → "how did that swimming session go?" → "you still dodging Disney?"), asking about events the user had ALREADY said didn't happen ("I didn't end up going" / "I never went swimming either"), and the persistent POV (`rex_pov`, correctly selected + injected into all 9 prompts) NEVER surfaced because every proactive slot was consumed by a follow-up and every reply was in answer-mode. ROOT CAUSE: `_post_response` fires one pending memory follow-up (`consciousness.get_pending_followup` → "how did <event> go?") after EVERY turn where Rex didn't just ask a question — and the gate is literally `if assistant_asked_question: hold, else: fire`. **The Roast rebalance (fewer reply-questions) INDIRECTLY unleashed this**: the `assistant_asked_question=False` branch now passes almost every turn, so the queue drains back-to-back as an interrogation. FIX (user chose "moderate"): a cadence clamp in `_post_response` so at most one follow-up fires per conversational lull — (1) **anti-repeat / no re-raise**: `_fired_followup_event_ids` (session set) drops any already-followed-up event from the popped queue, so a resolved event can't be re-raised even if the in-memory queue gets re-seeded from the DB in the window before the "didn't happen" reply is written (that race is how "you still dodging Disney?" came back after event 4 was resolved); (2) **cadence gate** `_memory_followup_cadence_allows()`: require ≥ `FOLLOWUP_MIN_GAP_EXCHANGES` (5; ~2 lines/turn ≈ 2–3 turns) transcript growth AND ≥ `FOLLOWUP_COOLDOWN_SECS` (60s) since the last fire — clocked on `conv_memory.get_session_transcript()` length so it SELF-RESETS when the transcript shrinks (new session), no reset-bundle plumbing needed; (3) **flat suppression** `FOLLOWUP_SUPPRESS_WHEN_FLAT` (True) → skip while `topic_thread.arc_reads_flat()`; (4) **"just declined" hold**: extended the existing top-of-`_post_response` `suppress_stale_followup` (was cancellation-only) to also fire on `_followup_event_did_not_happen(user_text)`, so a "didn't happen" reply holds the whole queue for that turn instead of pivoting straight to another remembered event. When gated, ALL popped follow-ups are re-queued (`set_pending_followup`) so nothing is lost — they fire in a later lull, which is ALSO what leaves the proactive slot open for Rex's own POV / idle-banter to win sometimes (the spacing is the POV's oxygen — same crowding-out shape as the roast, different crowder). `_note_memory_followup_fired(event_id)` records the fire (exchange clock + monotonic + fired-set) in the `if completed:` branch. The proactive event-memory FEATURE is intentionally kept (Rex remembering you mentioned a trip and asking how it went is a "feels alive" win) — only the VOLUME/cadence/listening was wrong. Knobs: `FOLLOWUP_MIN_GAP_EXCHANGES`, `FOLLOWUP_COOLDOWN_SECS`, `FOLLOWUP_SUPPRESS_WHEN_FLAT` (all in `config.py`). Tests (`MemoryFollowupCadenceTest`): first-allowed, gap block/allow, cooldown block/allow, flat suppression, session-reset clears state, `_note_*` records cadence+anti-repeat and ignores None. RE-TUNE LIVE: still interrogates → raise `FOLLOWUP_MIN_GAP_EXCHANGES` / `FOLLOWUP_COOLDOWN_SECS`; feels like Rex forgot your events → lower them. **Still needs a live `--gui`/robot pass** to confirm the felt change (less interrogation, POV finally surfacing). NOTE: (a) the reply cut-off was diagnosed + fixed in the next entry (ellipsis trail-off, NOT a logging artifact as first guessed); (b) running the test suite / any startup clears `logs/djr3x.log` + `logs/conversation.log` (config.py:100 clear-on-startup + RotatingFileHandler) — COPY a run's log before running tests if you need to analyze it.
- Ellipsis-trail-off cut-off + idle-banter volunteers POV + no invented props (do not regress; `intelligence/interaction.py` `_tail_is_speakable`/`_maybe_idle_banter`, `intelligence/social_frame.py` visual rule; `tests/test_streaming_tts.py` `TailIsSpeakableTest`, `tests/test_followup_resolution.py`, `tests/test_comedy_modes.py`). The SECOND 2026-06-04 live `--gui` run (with the cadence clamp above) surfaced three things. **(1) The reply cut-off (top user complaint, seen in BOTH 2026-06-04 runs).** Rex audibly stopped mid-sentence — "A flat 'hi'? I guess the excitement of" (the user even asked "the excitement of what?"), and earlier "…I mean, I've". ROOT CAUSE (confirmed by replaying the real splitter): the model TRAILS OFF with an ellipsis mid-clause ("I guess the excitement of…"). `_STREAM_TAIL_TERMINAL_RE` counts `…` as terminal, so `_tail_is_speakable` accepted the end-of-stream tail; then `_prepare_stream_sentence`→`llm.clean_response_text` does NOT strip it but TTS/normalization drops the ellipsis, so what's SPOKEN is the bare dangling fragment "I guess the excitement of" → a hard cut-off. (An em-dash or unpunctuated tail was already correctly dropped — only the ellipsis slipped through; `dropped incomplete stream tail` logged 0× that run, proving the tail was consumed not dropped.) FIX: `_tail_is_speakable` now drops an ELLIPSIS-terminated tail (`_ELLIPSIS_TAIL_RE` = `…` or `..`+) whose last word is a dangling connector/article/auxiliary (`_DANGLING_TAIL_WORDS` — `of/the/a/and/with/your/i've/…`); the complete earlier sentence ("A flat 'hi'?") still plays. Scoped to ELLIPSIS only and the set EXCLUDES words that can legitimately end a sentence (`this/to/is/that`), so a normal "You got this." / "I'd love to." is untouched (pinned by the existing `StreamTailTruncationTest` + new `TailIsSpeakableTest`). This also kills the downstream garble (the "the excitement of what?" → "The excitement of Or maybe…" resume only happened because the cut-off invited it). **(2) POV STILL never surfaced + Rex still interrogated** — but this run it was IDLE BANTER, not memory follow-ups: `_maybe_idle_banter` set `ask_user = (attempt == 0)` and `_idle_banter_count` resets every user turn (`_begin_user_turn`), so each quiet stretch only ever got the ask-FIRST attempt (the POV-volunteer branch, `attempt!=0`, almost never ran). Every idle re-engagement was an interview question ("So, what's been keeping your circuits buzzing lately?") and the selected POV (`sleep-is-a-bug`) never came out. FIX: flipped to volunteer-FIRST — `ask_user = (attempt != 0)`, directive `[0 if ask_user else 1]`, so the first re-engagement VOLUNTEERS the `rex_pov.active_pov_text()` preoccupation and only a later banter in the same stretch asks. Division of labor: the reply path already carries curiosity about the user; idle banter is where Rex brings his OWN thing (the design intent — see [[rex-exhaustion-is-layered]], [[rex-conversation-design-intent]]). Falls back to the generic volunteer directive when POV is off/empty; `frame.allow_question=bool(ask_user)` still gates it. **(3) Invented a "drink" again** ("…sipping your drink") — the Roast-rebalance no-hallucination guardrail held for most of the run, but `social_frame.build_directive`'s Visual permission literally LISTED "the drink in their hand" as an example of visual material to riff on (vision is scene/people/face — never detects a drink), priming the invention in BOTH 2026-06-04 runs. FIX: dropped that example and turned it into a NEGATIVE one ("Only what's genuinely there, though: never invent a prop or detail — a drink in their hand, what they're wearing or holding — to set up a joke"), reinforcing the core-prompt guardrail at the point of temptation. (The cut-off fix likely prevents THIS instance anyway since the drink rode the garbled resume; this is defense-in-depth.) RE-TUNE LIVE: idle banter feels too self-absorbed / never asks → revert `ask_user` to `attempt==0` or alternate on a session-level counter; a legit ellipsis trail-off got dropped → trim `_DANGLING_TAIL_WORDS`. All three are kill-switch-free prompt/logic tweaks; suite green at 863.
- LLM-in-the-loop conversation-quality eval (do not regress; `evals/run_quality_eval.py`, `evals/checkers.py`, `evals/quality_corpus.json`, `evals/README.md`). The point of the whole 2026-06-04 arc: stop the divergent "run the robot → read one log → patch one bad line" workflow (it never converges — the dangling-word list needed a new word every time the model trailed off differently), and instead MEASURE generation failure CLASSES across a corpus. Runs scenarios through the ACTUAL reply path — `conversation_agenda.build_turn_plan` → `social_frame.build_frame` → `llm.stream_response` (gpt-4o-mini) → the SAME `interaction._split_stream_sentences` + `_tail_is_speakable` assembly the speech queue uses → `social_frame.govern_response` → `comedy_modes.polish_response` — so it scores what Rex would actually SAY (the faithful spoken text, incl. the ellipsis-tail fix and the banned-opener strip; the polish matters — without it `banned_opener` over-reports 25% vs the true 0%). CHECKERS (`checkers.py`, each returns `Finding(cls, flagged, detail)` or None=N/A): DETERMINISTIC — `over_questioning` (>1 `?`), `cantina_bleed` (regex for cantina/Oga's/Batuu — the backstory bleed), `banned_opener` (`Ah,`/`Oh,`/`Well, well`), `trail_off` (spoken reply ends without terminal punct — the cut-off class, should stay 0%), `re_asks` (a reply question's content-word overlap ≥0.6 with a prior Rex question); LLM-JUDGE (one cheap gpt-4o-mini JSON call, FAILS SAFE → flagged=False + `judge_error` detail on any error) — `invented_prop` (asserts a held/worn prop with no visual data) and `roasted_sincere` (mocks a sincere share / needles a boundary; only on scenarios marked `"user_sincere": true`). Data-driven: add a scenario to `quality_corpus.json` (no Python; same spirit as the structural `tests/fixtures/conversation_replays.json`). **OPT-IN, makes REAL OpenAI calls** (~1 generation + a couple judges per reply; full corpus @ `--samples 1` ≈ 20 cheap calls) — so it lives in `evals/` (NOT `tests/`) and is **NEVER run by `unittest discover -s tests`** (that suite stays network-free; `evals/` has no `test*.py` so discovery skips it even without `-s`). Flags: `--samples N` (rates are NOISY at low N — generation is non-deterministic, `roasted_sincere` swung 22%→56% across two 3-sample runs; use 5+ to compare before/after), `--only <substr>`, `--out <json>`, `--gate <rate>` (exit 1 if any class exceeds → CI/regression gate). Complements, does NOT replace, the deterministic `tests/test_conversation_replay.py` (that asserts GOVERNOR decisions with no LLM; this measures GENERATION quality with the LLM). **BASELINE (2026-06-04, `--samples 3`, 8 scenarios):** `roasted_sincere` **22–56%** = the DOMINANT remaining issue (the Roast rebalance helped but did NOT solve it — sincere shares still get roasted at a high rate; this is the next thing to fix, and the eval now measures the fix); `cantina_bleed` ~8% (minor); `invented_prop`/`trail_off`/`over_questioning`/`re_asks`/`banned_opener` all **0%** — corpus-scale VALIDATION that the recent fixes hold (not just one good live run). Fast-follows: grow the corpus from real `logs/conversation.log` transcripts, add classes (dead-end ack, repeated joke, wrong-addressee), and use `--gate` as a pre-commit/CI regression guard once rates are stabilized with higher `--samples`.
- Boundary handling — don't roast a boundary / withdrawal (do not regress; `intelligence/social_frame.py` `_BOUNDARY_RE`/`_looks_like_boundary`/`_roast_level`, `intelligence/conversation_agenda.py` boundary branch; `tests/fixtures/conversation_replays.json` + `tests/test_comedy_modes.py` `BoundaryDetectionTest`). FIRST eval-driven fix (the model use of the quality eval above): the `--samples 8` baseline put `roasted_sincere` at **46%** (11/24) and the flagged replies showed **8/11 were the user saying "I'll be quiet"** — Rex needled the boundary relentlessly ("How very droid of you", "I'd rate it a solid 3 out of 10", "Are you sure you don't want to share?"). ROOT CAUSE: empathy reads "I'll be quiet" as neutral (not withdrawn), so it slipped past `_roast_level`'s affect gate into ROAST-LEAN; and nothing told Rex to RESPECT the boundary, so even with the roast suppressed he RESISTED it ("silence isn't my jam, I thrive on noise"). FIX (two parts, each measured): (1) `social_frame._looks_like_boundary` (a tuned `_BOUNDARY_RE` covering "I'll be quiet / I'd rather not / give me a minute / change the subject / maybe later / I'll pass / not in the mood", validated against false positives like "I'd rather go to the quiet bar") → `_roast_level` now takes `user_text` and returns `"none"` for a boundary (gate placed FIRST, before the affect/pref checks); (2) a new boundary BRANCH in `conversation_agenda` (before the reassurance branch; reuses `social_frame._looks_like_boundary` via lazy import — agenda→social_frame is the existing one-way dependency) that sets `plan.purpose="boundary"` and emits a POSITIVE directive: "RESPECT it immediately: one short, warm acknowledgement that gives them room, then stop. Do NOT push back, protest, complain you prefer noise/chatter, needle, or roast the boundary." MEASURED TRAJECTORY (`roasted_sincere`, full corpus @ `--samples 8`): **46% → 29%** (roast-off alone) **→ 21%** (+ the respect-it directive); the boundary scenario ALONE went **100% (8/8) → 8% (1/12)** (`--only boundary --samples 12`), and its cantina-bleed fallback dropped to 0% too (when Rex didn't know how to handle the boundary he reached for cantina riffs). STOPPED at 21% DELIBERATELY: the remaining fails are mostly BORDERLINE — two "boundary fails" actually respect it well ("Got it. I'll be over here running diagnostics", "Noted. Enjoy the silence while it lasts") and the back-pain ones engage-then-lightly-tease — i.e. the JUDGE is somewhat strict on the in-character flourish the design WANTS to keep; driving 21%→0% would make Rex a bland yes-droid, violating [[rex-conversation-design-intent]]. This is the eval-driven loop working as intended: change → measure the class rate → keep the fix that clearly helps (boundary needling 100%→8%) → STOP when further tuning trades against the edge. Deterministic, no latency, no kill switch needed (additive gate + one prompt branch). Tests: replay corpus asserts `purpose=boundary`/`allow_roast=none`/directive includes "respect it"/"boundary"/"push back" for "I'll be quiet" and "can we change the subject" (no LLM); `BoundaryDetectionTest` pins the regex (positive + the quiet-bar false-positive) and the roast→none easing. Suite green at **866**. Known eval caveat surfaced here: the `roasted_sincere` judge (gpt-4o-mini) over-flags boundary-respecting replies that carry a light self-directed flourish — a judge-calibration fast-follow (don't tune it in the same session you grade a fix with). `cantina_bleed` is still ~8% corpus-wide = the next eval-measured target.

## Likely Future Work

- Decide whether the streaming answer path is sufficient latency cover on its own, or whether to re-enable (and tune) the slow-path ack / latency filler for the slowest paths.
- Deeper conversation steering: detect a topic shift semantically (not just explicit "I like / I'm building X") and update/expire the active interest accordingly; today a new subject the user is clearly engaged in but doesn't name in an interest form is not picked up.
- Add directional audio support for stereo ReSpeaker Lite input.
- Improve group turn triage for crosstalk and ambiguous addressees.
- Continue reducing OpenAI calls on common conversational paths.
- Expand tests around identity introduction, GUI text mode, no-audio mode, and multi-speaker ambiguity.
